// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

import { RowDecorations } from '../gitwire/row-decorations.js';


export class PackedCommitChunk {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):PackedCommitChunk {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsPackedCommitChunk(bb:flatbuffers.ByteBuffer, obj?:PackedCommitChunk):PackedCommitChunk {
  return (obj || new PackedCommitChunk()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsPackedCommitChunk(bb:flatbuffers.ByteBuffer, obj?:PackedCommitChunk):PackedCommitChunk {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new PackedCommitChunk()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

from():number {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

to():number {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

shaWidthBytes():number {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : 0;
}

shas(index: number):number|null {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.readUint8(this.bb!.__vector(this.bb_pos + offset) + index) : 0;
}

shasLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

shasArray():Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? new Uint8Array(this.bb!.bytes().buffer, this.bb!.bytes().byteOffset + this.bb!.__vector(this.bb_pos + offset), this.bb!.__vector_len(this.bb_pos + offset)) : null;
}

parentOffsets(index: number):number|null {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.readUint8(this.bb!.__vector(this.bb_pos + offset) + index) : 0;
}

parentOffsetsLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

parentOffsetsArray():Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? new Uint8Array(this.bb!.bytes().buffer, this.bb!.bytes().byteOffset + this.bb!.__vector(this.bb_pos + offset), this.bb!.__vector_len(this.bb_pos + offset)) : null;
}

parentShas(index: number):number|null {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? this.bb!.readUint8(this.bb!.__vector(this.bb_pos + offset) + index) : 0;
}

parentShasLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

parentShasArray():Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? new Uint8Array(this.bb!.bytes().buffer, this.bb!.bytes().byteOffset + this.bb!.__vector(this.bb_pos + offset), this.bb!.__vector_len(this.bb_pos + offset)) : null;
}

identityIds(index: number):number|null {
  const offset = this.bb!.__offset(this.bb_pos, 16);
  return offset ? this.bb!.readUint8(this.bb!.__vector(this.bb_pos + offset) + index) : 0;
}

identityIdsLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 16);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

identityIdsArray():Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 16);
  return offset ? new Uint8Array(this.bb!.bytes().buffer, this.bb!.bytes().byteOffset + this.bb!.__vector(this.bb_pos + offset), this.bb!.__vector_len(this.bb_pos + offset)) : null;
}

times(index: number):number|null {
  const offset = this.bb!.__offset(this.bb_pos, 18);
  return offset ? this.bb!.readUint8(this.bb!.__vector(this.bb_pos + offset) + index) : 0;
}

timesLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 18);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

timesArray():Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 18);
  return offset ? new Uint8Array(this.bb!.bytes().buffer, this.bb!.bytes().byteOffset + this.bb!.__vector(this.bb_pos + offset), this.bb!.__vector_len(this.bb_pos + offset)) : null;
}

subjectBytes(index: number):number|null {
  const offset = this.bb!.__offset(this.bb_pos, 20);
  return offset ? this.bb!.readUint8(this.bb!.__vector(this.bb_pos + offset) + index) : 0;
}

subjectBytesLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 20);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

subjectBytesArray():Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 20);
  return offset ? new Uint8Array(this.bb!.bytes().buffer, this.bb!.bytes().byteOffset + this.bb!.__vector(this.bb_pos + offset), this.bb!.__vector_len(this.bb_pos + offset)) : null;
}

subjectOffsets(index: number):number|null {
  const offset = this.bb!.__offset(this.bb_pos, 22);
  return offset ? this.bb!.readUint8(this.bb!.__vector(this.bb_pos + offset) + index) : 0;
}

subjectOffsetsLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 22);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

subjectOffsetsArray():Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 22);
  return offset ? new Uint8Array(this.bb!.bytes().buffer, this.bb!.bytes().byteOffset + this.bb!.__vector(this.bb_pos + offset), this.bb!.__vector_len(this.bb_pos + offset)) : null;
}

dictionaryBase():number {
  const offset = this.bb!.__offset(this.bb_pos, 24);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

dictionary(index: number):string
dictionary(index: number,optionalEncoding:flatbuffers.Encoding):string|Uint8Array
dictionary(index: number,optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 26);
  return offset ? this.bb!.__string(this.bb!.__vector(this.bb_pos + offset) + index * 4, optionalEncoding) : null;
}

dictionaryLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 26);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

decorations(index: number, obj?:RowDecorations):RowDecorations|null {
  const offset = this.bb!.__offset(this.bb_pos, 28);
  return offset ? (obj || new RowDecorations()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

decorationsLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 28);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

static startPackedCommitChunk(builder:flatbuffers.Builder) {
  builder.startObject(13);
}

static addFrom(builder:flatbuffers.Builder, from:number) {
  builder.addFieldInt32(0, from, 0);
}

static addTo(builder:flatbuffers.Builder, to:number) {
  builder.addFieldInt32(1, to, 0);
}

static addShaWidthBytes(builder:flatbuffers.Builder, shaWidthBytes:number) {
  builder.addFieldInt8(2, shaWidthBytes, 0);
}

static addShas(builder:flatbuffers.Builder, shasOffset:flatbuffers.Offset) {
  builder.addFieldOffset(3, shasOffset, 0);
}

static createShasVector(builder:flatbuffers.Builder, data:number[]|Uint8Array):flatbuffers.Offset {
  builder.startVector(1, data.length, 1);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addInt8(data[i]!);
  }
  return builder.endVector();
}

static startShasVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(1, numElems, 1);
}

static addParentOffsets(builder:flatbuffers.Builder, parentOffsetsOffset:flatbuffers.Offset) {
  builder.addFieldOffset(4, parentOffsetsOffset, 0);
}

static createParentOffsetsVector(builder:flatbuffers.Builder, data:number[]|Uint8Array):flatbuffers.Offset {
  builder.startVector(1, data.length, 1);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addInt8(data[i]!);
  }
  return builder.endVector();
}

static startParentOffsetsVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(1, numElems, 1);
}

static addParentShas(builder:flatbuffers.Builder, parentShasOffset:flatbuffers.Offset) {
  builder.addFieldOffset(5, parentShasOffset, 0);
}

static createParentShasVector(builder:flatbuffers.Builder, data:number[]|Uint8Array):flatbuffers.Offset {
  builder.startVector(1, data.length, 1);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addInt8(data[i]!);
  }
  return builder.endVector();
}

static startParentShasVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(1, numElems, 1);
}

static addIdentityIds(builder:flatbuffers.Builder, identityIdsOffset:flatbuffers.Offset) {
  builder.addFieldOffset(6, identityIdsOffset, 0);
}

static createIdentityIdsVector(builder:flatbuffers.Builder, data:number[]|Uint8Array):flatbuffers.Offset {
  builder.startVector(1, data.length, 1);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addInt8(data[i]!);
  }
  return builder.endVector();
}

static startIdentityIdsVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(1, numElems, 1);
}

static addTimes(builder:flatbuffers.Builder, timesOffset:flatbuffers.Offset) {
  builder.addFieldOffset(7, timesOffset, 0);
}

static createTimesVector(builder:flatbuffers.Builder, data:number[]|Uint8Array):flatbuffers.Offset {
  builder.startVector(1, data.length, 1);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addInt8(data[i]!);
  }
  return builder.endVector();
}

static startTimesVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(1, numElems, 1);
}

static addSubjectBytes(builder:flatbuffers.Builder, subjectBytesOffset:flatbuffers.Offset) {
  builder.addFieldOffset(8, subjectBytesOffset, 0);
}

static createSubjectBytesVector(builder:flatbuffers.Builder, data:number[]|Uint8Array):flatbuffers.Offset {
  builder.startVector(1, data.length, 1);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addInt8(data[i]!);
  }
  return builder.endVector();
}

static startSubjectBytesVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(1, numElems, 1);
}

static addSubjectOffsets(builder:flatbuffers.Builder, subjectOffsetsOffset:flatbuffers.Offset) {
  builder.addFieldOffset(9, subjectOffsetsOffset, 0);
}

static createSubjectOffsetsVector(builder:flatbuffers.Builder, data:number[]|Uint8Array):flatbuffers.Offset {
  builder.startVector(1, data.length, 1);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addInt8(data[i]!);
  }
  return builder.endVector();
}

static startSubjectOffsetsVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(1, numElems, 1);
}

static addDictionaryBase(builder:flatbuffers.Builder, dictionaryBase:number) {
  builder.addFieldInt32(10, dictionaryBase, 0);
}

static addDictionary(builder:flatbuffers.Builder, dictionaryOffset:flatbuffers.Offset) {
  builder.addFieldOffset(11, dictionaryOffset, 0);
}

static createDictionaryVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startDictionaryVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static addDecorations(builder:flatbuffers.Builder, decorationsOffset:flatbuffers.Offset) {
  builder.addFieldOffset(12, decorationsOffset, 0);
}

static createDecorationsVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startDecorationsVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static endPackedCommitChunk(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  builder.requiredField(offset, 10) // shas
  builder.requiredField(offset, 12) // parent_offsets
  builder.requiredField(offset, 14) // parent_shas
  builder.requiredField(offset, 16) // identity_ids
  builder.requiredField(offset, 18) // times
  builder.requiredField(offset, 20) // subject_bytes
  builder.requiredField(offset, 22) // subject_offsets
  builder.requiredField(offset, 26) // dictionary
  builder.requiredField(offset, 28) // decorations
  return offset;
}

static createPackedCommitChunk(builder:flatbuffers.Builder, from:number, to:number, shaWidthBytes:number, shasOffset:flatbuffers.Offset, parentOffsetsOffset:flatbuffers.Offset, parentShasOffset:flatbuffers.Offset, identityIdsOffset:flatbuffers.Offset, timesOffset:flatbuffers.Offset, subjectBytesOffset:flatbuffers.Offset, subjectOffsetsOffset:flatbuffers.Offset, dictionaryBase:number, dictionaryOffset:flatbuffers.Offset, decorationsOffset:flatbuffers.Offset):flatbuffers.Offset {
  PackedCommitChunk.startPackedCommitChunk(builder);
  PackedCommitChunk.addFrom(builder, from);
  PackedCommitChunk.addTo(builder, to);
  PackedCommitChunk.addShaWidthBytes(builder, shaWidthBytes);
  PackedCommitChunk.addShas(builder, shasOffset);
  PackedCommitChunk.addParentOffsets(builder, parentOffsetsOffset);
  PackedCommitChunk.addParentShas(builder, parentShasOffset);
  PackedCommitChunk.addIdentityIds(builder, identityIdsOffset);
  PackedCommitChunk.addTimes(builder, timesOffset);
  PackedCommitChunk.addSubjectBytes(builder, subjectBytesOffset);
  PackedCommitChunk.addSubjectOffsets(builder, subjectOffsetsOffset);
  PackedCommitChunk.addDictionaryBase(builder, dictionaryBase);
  PackedCommitChunk.addDictionary(builder, dictionaryOffset);
  PackedCommitChunk.addDecorations(builder, decorationsOffset);
  return PackedCommitChunk.endPackedCommitChunk(builder);
}
}

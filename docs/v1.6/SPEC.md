# Kira Studio — v1.6

v1.5 shipped the code intelligence subsystem as one cohesive chapter (`docs/v1.5/SPEC.md`, C1-C14).
This chapter returns to v1.1/v1.2/v1.4's own shape: six independent, unrelated phases across
existing modules, so it continues `P` numbering rather than taking a fresh letter — the same reason
v1.4's own opening paragraph gives for reusing `P` instead of picking a new one.

**Six rows today.** As with every earlier chapter, this table accrues findings/splits as phases
land; a row may gain a "Deliverable" paragraph reporting what was actually built if it diverges from
what's written here, per this project's own never-retro-edit convention.

**Sequencing.** P60 (CodeMirror removal) goes first because two later rows plausibly touch editing
surfaces it will have already moved onto Monaco (P65's cell editor). P61 (dependency/runtime
upgrade) goes second, deliberately right after P60 rather than first or last: P60 already touches
`package.json` removing a whole dependency family, so doing the broader version bump immediately
after — on the tree P60 leaves, not before it — avoids two independent large dependency churns
landing at once. P62-P65 are independent of each other and of P60/P61, ordered as given.

| Phase | Deliverable | Why here |
|---|---|---|
| **P60 Editor consolidation: remove CodeMirror, Monaco everywhere** | Remove `@codemirror/*` (`autocomplete`, `commands`, `lang-json`, `lang-sql`, `lang-xml`, `language`, `lint`, `merge`, `state`, `view` — all present in root `package.json` today) entirely, replacing every surface that uses them with Monaco, which v1.5 already brought into this app for the native code workspace (`apps/kira-studio/frontend/src/views/repo/monaco.ts`'s lazy-load pattern, `language.ts`'s language registration). Confirmed CodeMirror usage sites, checked directly rather than assumed: a shared `editor/CodeMirrorHost.vue` component (with `editor/{theme,languages,wrapSelection,findRanges,hover,diagnostics,variableHighlight}.ts` supporting it) mounted by the SQL query console (`views/console/*` — `sqlLanguageService.ts`, `completion.ts`, `sqlHover.ts`, `sqlDiagnostics.ts`, `ddl.ts`, `lezerNodes.ts` all reference Lezer/CodeMirror-specific APIs) and `project/SchemaDialog.vue`'s DDL editor. `@codemirror/merge`'s presence in the dependency list means a diff/merge view exists somewhere on CodeMirror too, despite Monaco already owning diff rendering in the repo workspace (v1.5 C6) — this phase's own planning pass finds every such site, not just the two confirmed here, and must account for every capability each site actually depends on: SQL-aware autocomplete over live schema metadata (`state/schemaColumns.ts`), lint/diagnostics, DDL-document completion, hover, and whatever the merge view provides. "Make sure all the features still work" is the acceptance bar, stated by the request itself, not a nice-to-have: this phase does not ship until every one of those capabilities has a working Monaco-side equivalent, verified feature-by-feature, not just "the editor renders" | First: it is the largest, most cross-cutting change in this chapter, several later phases (P65) touch a cell editor that may itself be CodeMirror-based, and doing it first means every phase after builds on the app's editing surface in its final, single-engine state rather than a moving target |
| **P61 Dependency and runtime upgrade** | Bring the toolchain and every major dependency to their current latest stable versions. Confirmed today, checked directly: Go `1.27.0` (root `go.mod`), Wails pinned to a **pre-release** `github.com/wailsapp/wails/v3 v3.0.0-beta.16` — a beta dependency this whole app builds on, worth explicit attention rather than a routine bump, since a Wails v3 stable release (or a beta-to-beta jump) can carry breaking API changes to `internal/shell`'s own usage. This phase's own planning pass builds the full current-vs-latest inventory (Go direct dependencies, every frontend package across the Bun workspace — `vue`, the Vite/rolldown build tooling, `monaco-editor`, `slickgrid`, `flatbuffers`, `zod`, tree-sitter grammars, `@vscode/codicons`, `fuzzysort` — and the Bun/Node runtime itself) that this opening row deliberately does not — a version table goes stale between planning and implementation, so it belongs in the plan doc, checked at plan time, not copied here now. Any deliberate historical version pin found in code comments or `docs/DEV_ENVIRONMENT.md` (a compatibility workaround, not mere staleness) must be investigated and either resolved or explicitly re-affirmed with its reason restated, never silently bumped past | Second, immediately after P60: P60 already disturbs `package.json` by removing a whole dependency family, so the broader bump lands on that already-changed tree as one deliberate pass rather than as a second large, independent dependency churn competing with the first |
| **P62 Git blame widget in the native git module** | Port git blame into the natively-embedded git module (v1.5 C10/C11's read-only `packages/git-ui` mount) — **not new backend work**: v1.4's own P5 already built this once, for the VS Code extension's status bar (`internal/gitrpc`'s `blame.line` method, a porcelain-parser-backed `git blame` run through `internal/gitclient`'s streaming runner, the `packages/git-ipc` contract method it added). Confirmed `blame.line` is already present in v1.5's `internal/bridge/gitstream.go` read-only allowlist, admitted as a pure read — so the wire/backend plumbing this phase needs already exists and is already safe to call from the native surface; the work is a native-side UI consumer, following the same porting discipline C11 used for the review layer (reuse `packages/git-ui`'s own components/state where they already exist, build only what the native embedding genuinely lacks that the extension's own surface — a status-bar item, per P5 — doesn't need in the same shape here). This phase's own planning pass decides where blame surfaces in the native workspace (inline gutter annotations over Monaco, matching C11's comment-decoration precedent, vs. a status-bar-style summary matching the extension's original shape, vs. both) and whether `packages/git-ui` already carries any blame-consuming component from P5 worth reusing directly, or whether P5's blame consumer lived entirely in the extension's own code (`apps/kira-studio-vscode/src/extension.ts`) with nothing in the shared package to port | Independent of every other row in this chapter; sequenced third only because it was named second in the request |
| **P63 S3 and Redis browse panels: vertical split, relocated back-navigation, clearer Redis types** | Two related but independent UI reworks to the existing S3 and Redis browse panels (locations not yet confirmed — this phase's own planning pass finds the actual current components, likely under `apps/kira-studio/frontend/src/views/keyvalue/` for Redis and a to-be-located S3 equivalent). Deliverable, per the request stated directly: (1) both panels move to a vertical split-screen layout with the data view, replacing whatever layout each currently uses; (2) the current back/up-navigation control (today apparently a labeled button positioned at the top of the panel) becomes a left-arrow icon, consistent in placement with an inline back-navigation affordance rather than a top-of-panel button; (3) Redis specifically gains clearer visual differentiation of its data types (string, hash, list, set, sorted set, stream) in both the key list and the value view, modeled on a real standalone Redis client's own presentation (this phase's own planning pass names which reference client(s) it modeled the design on and why). This phase's own planning pass must first establish the actual current layout of both panels precisely (read the real template code, not assume from the component name) and check whether an existing split-pane/panel-shell primitive already used elsewhere in this app (`PanelShell` and its kin, per v1.5's own precedent) is the right vehicle for consistency with the rest of the app's browsing surfaces | Independent of every other row |
| **P64 Update-availability banner (no silent auto-update)** | The app is distributed unsigned, so a real auto-download-and-install update mechanism is not viable. This phase instead: fetch the latest GitHub release for this repository, compare it against the running app's own version (`internal/buildinfo`, confirmed to already hold build/version metadata), and — only when a newer release exists — show a banner in the status bar (a real, existing UI region: confirmed referenced in `TitleBar.vue`/`WorkbenchShell.vue`/`SettingsDialog.vue`) that opens the GitHub releases page in the user's OS-default browser when clicked. This phase's own planning pass confirms whether `internal/ghclient` (already used by the git module's `commit.resolvePr`/`branch.resolvePr`) already exposes, or can be extended with, an anonymous "list releases" call (public release metadata needs no per-user credential), decides the check's cadence (on launch, periodically, both), and finds this app's existing mechanism (if any) for opening a URL in the OS browser from a Wails window rather than an in-app webview | Independent of every other row |
| **P65 Cell editor: enter edit mode for a foreign-key preview's related record** | Extends the existing FK-preview feature (clicking a foreign-key-valued cell already shows the referenced row from its own table — confirmed present, exact component not yet located; likely `views/shared/celleditor/CellEditorView.vue` or adjacent) so the preview can also open that related record in a real edit session, not only a read-only preview. This phase's own planning pass locates the current FK-preview flow precisely (what it calls today to fetch the related row, what schema/FK-relationship metadata it already has access to) and the app's existing row-edit surface (wherever a normal record is edited today), and designs routing the FK preview into that same edit surface pointed at the related table/row, reusing it rather than building a second edit UI. If P60 has already moved the cell editor itself onto Monaco, this phase builds on that end state | Independent of every other row; sequenced last since it is the one most likely to interact with whatever P60 leaves the cell editor's own editing surface looking like |

## Layout

- **`SPEC.md`** — this file, one row per phase, updated as phases land or split.
- **`plans/`** — one implementation plan per phase, committed before that phase's implementation
  starts, written by an Opus subagent per `CLAUDE.md`'s own process, never edited afterward.
- **`mcp-repo-map-issues.md`** — this chapter's own repo-map MCP dogfooding log, continuing the
  practice v1.5 established (`CLAUDE.md`'s "Repo-map MCP server" section, updated to point here).

## Closing

After all six phases land: **two rounds of code review**, per `CLAUDE.md`'s own process — each round
three parallel Opus subagents (architecture/security, functional correctness, performance/resource
efficiency), findings-only, then one sequential Sonnet subagent fixes every finding. Run the full
cycle twice, not once with the second treated as optional.

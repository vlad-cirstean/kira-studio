# Kira Studio — v1.2

v1.1 shipped: the SlickGrid grid-engine migration, the connection auth/test-matrix hardening work,
and a full post-migration code review, closed out through P29 (`docs/v1.1/SPEC.md`). This chapter's
headline is a new, user-directed feature area: an **HTTP client** — request/response tooling for
HTTP and gRPC, living alongside the existing database client inside the same app.

## What this chapter is about

The app gains a second top-level mode, **Http** (renamed to **Api** in P12 — see the module-boundary
section below; every other reference in this document still says "Http", the name in the code as of
this writing), sitting beside the existing **Studio** mode (the database client this app has always
been) behind a **custom title bar** (matching the app's own background color, not the OS chrome)
carrying the two mode tabs. Http mode is a full request client:

- **Protocols**: HTTP and gRPC only (no other protocol families in scope for this chapter —
  WebSocket was considered and explicitly dropped from scope for now).
- **Collections**, organized in a left-hand panel the same way Studio's connection tree occupies
  that position today, stored on disk in **Postman Collection format** — with **import and export**
  of that format, so a user's existing Postman collections are portable in and out of this app.
- **curl**: parse a pasted curl command into a request, and generate a curl command from the current
  request.
- **Response history** — past responses are kept and browsable, not just the latest one.
- **Raw inspection and a raw request editor** — the exact bytes sent and received are inspectable,
  and a request can be authored/edited at that raw level, not only through the structured
  method/headers/body form.
- **Status-code hints** — a short explanation of what a response's HTTP status code means, shown
  alongside it.
- **JSON handling** — syntax highlighting and a beautify (pretty-print) action for JSON bodies, both
  in requests and responses.
- **A full set of request body formats** (P3) — raw plain text, a syntax-highlighted/beautifiable
  Code mode (JavaScript, JSON, HTML, XML), form-data with real file fields, x-www-form-urlencoded,
  and binary (one local file as the whole body). Started as a Postman-parity set including GraphQL;
  GraphQL was later removed and raw's language sub-selector split into its own Code mode, so the
  mode vocabulary is this app's own rather than Postman's `body.mode` verbatim — P4's collection
  import/export is where that boundary gets a real translation.
- **Collection variables and environments** — `{{name}}` values resolved wherever a request
  references one, with a **history of prior values** for both, a **secret** checkbox per entry
  (masked inline, revealed only after a system authentication prompt — the same fingerprint/Touch ID
  confirmation this app already built for saved connection credentials), and **reordering**.
- **Faker-backed dynamic values** — a Postman/Bruno-style `{{$...}}` dynamic-variable form that
  resolves to a freshly generated value (a random name, email, UUID, timestamp, and so on) from
  `@faker-js/faker` each time a request is sent, rather than a stored value.
- **A request timeline** — a step-by-step breakdown of what happened while a request ran (DNS,
  connect, TLS handshake, wait/TTFB, content download, and so on), the way Postman's own timing tab
  works, not just a single elapsed-time number.

**The load-bearing constraint, stated up front because it decides the whole chapter's opening
phase**: most of the chrome Http mode needs already exists in Studio mode, just wired to Studio's
own data — a left-hand tree panel, a bottom/tabbed content area for open items, and the title bar
itself. None of that is written twice. **P1 modularizes those shared surfaces first** — the left
panel, the tab strip/content area, and the title bar itself — into components parameterized by mode,
so Studio keeps working unchanged on top of the same modularized panels Http is then built on,
rather than Http growing a second, parallel copy of chrome Studio already has. Every phase after P1
builds Http's own functionality on top of that shared shell.

## Studio/Http module boundary

A cross-cutting constraint for every phase in this chapter, not just its own row below: Http mode's
code should stay as separable from Studio's as practical, organized so that if this app were ever
split into two separate apps, pulling Http out would be a mechanical move rather than an untangling.
Concretely — Http-specific frontend code lives under its own directories (`frontend/src/http/`,
`frontend/src/views/httprequest/`, and so on) rather than interleaved file-by-file with Studio's own;
Http-specific Go code stays in its own packages (`internal/httpclient`, `internal/postman`, and so
on) rather than folded into Studio's; and **test suites for the two modules are kept in separate
directories/files even though the existing `test:ui`/`test:unit` commands keep running both
together** — a single test command covering both is fine, a single test *file* covering both is not.
No phase should merge Http and Studio code into one shared file where a per-module file or directory
would do instead. P12 below is the phase that goes back over the whole chapter and audits/tightens
this boundary end to end, but every phase from here on should default to the separated shape as it's
written rather than accumulate coupling for P12 to undo later.

**The target is a genuine workspace package, not just a tidy directory.** v1.3 (the Git module,
`docs/v1.3/SPEC.md`) sets the precedent this chapter's P12 now matches: real Bun workspace packages
with their own `package.json` (root `workspaces` gains a `packages/*` entry — today's `packages/`
directory, e.g. `packages/shared`, is source-only, reached via a `@shared/*` path alias with no
independent `package.json`, build, or test; that's the weaker shape this phase upgrades past, not
the one to copy), so the Api-facing frontend code is independently buildable and testable apart from
`apps/kira-studio`'s one frontend project — the same "point a new host at this package" property
that makes the split-into-a-separate-app goal real rather than aspirational. A directory move alone
does not satisfy this boundary; P12's plan is where the package split (`packages/api-core`,
`packages/api-ui`, or whatever naming its own plan settles on) is designed in full.

**The module is also renamed Http → Api in P12** — visually (the mode tab's own label, and any other
user-facing "Http" text naming the mode itself) and in code wherever an identifier, file, or
directory names *the module*, not the protocol: `frontend/src/http/` → `frontend/src/api/`,
`views/httprequest/` → `views/apirequest/` (exact naming left to P12's own plan), the mode-tab
string, and so on. This does **not** touch places where "Http" correctly names the HTTP *protocol*
rather than the module — `internal/httpclient` (the HTTP transport), `HttpBodyWire`/`httpMethodClass`
and similar protocol-shaped identifiers, and any doc prose describing HTTP the protocol all keep
saying "Http"/"HTTP", since the module also hosts gRPC (P11) and "Api" is the name for that whole
mode, not a synonym for "Http". P12's own plan is where the exact line between the two gets drawn
file by file — this paragraph states the intent, not the full list.

**Separation does not mean reimplementing shared infrastructure per module.** v1.3's own P1
(`docs/v1.3/SPEC.md`) had to build a real correlated-RPC-with-credits protocol in Go for the Git
stream, because nothing already existed there to reuse — that was genuine new capability, not
duplication. The corrected principle, now stated there and repeated here so P12 doesn't rediscover
it: once a piece of infrastructure is generic rather than module-specific (a transport protocol, a
reveal-gate flow, and so on), it belongs in its own shared package/module the first time a second
consumer is foreseeable, not duplicated per module and not deferred until that second consumer
already exists. P5's own OQ-2 (the secret-reveal flow `internal/localauth` gates, needed by both a
connection reveal in `project/` and a variable reveal in `http/`, with no shared home either can
import without recreating the coupling this boundary exists to avoid) is exactly this situation —
P12 is where that gets a real shared home rather than staying two copies with a comment each.

## Phasing

This is a starting decomposition, not a fixed contract — `P1`'s own modularization work is expected
to reveal exactly which pieces of the existing panel/tab machinery generalize cleanly and which
don't, and later rows here may be re-scoped by their own plan docs once that's known. Each row still
gets its own Opus-authored plan under `plans/` before implementation starts, per `AGENTS.md`.

| Phase | Deliverable | Why here |
|---|---|---|
| **P1 Shared UI shell: title bar, left panel, and tab/content area, modularized** | A custom title bar (app background color, not OS chrome) hosting the two top-level mode tabs (Studio / Http). Generalize the existing left-hand tree panel and the bottom tabbed content area into components parameterized by what they're showing, so a *different* left panel (a collections tree) and a *different* tab/content kind (HTTP request/response tabs) can be hosted through the same underlying panel and tab-strip machinery Studio already uses — without Studio's own behavior changing. No new user-facing HTTP functionality yet; this is purely the structural work every later phase depends on | Explicitly called out as the necessary first step: most of Http mode's chrome is chrome Studio already has, and building it twice would mean two panel implementations to maintain and two places for the same bug to exist. Doing this before any HTTP-specific feature work means every phase after it builds on the real shared shell instead of a placeholder |
| **P2 HTTP request/response core** | The minimum viable Http tab: a request builder (method, URL, query params, headers, body) sending real HTTP requests, a response viewer (status, headers, body) with JSON syntax highlighting and a beautify action, and status-code hint text shown alongside the response status | The smallest end-to-end slice of the feature — request in, response out, readable — everything after this phase adds capability on top of a working core rather than building toward one |
| **P3 Request body payload formats** | Extend the request body editor from P2 to five modes: **raw** (plain text only), **code** (JavaScript/JSON/HTML/XML, syntax-highlighted, JSON/XML also beautifiable), **x-www-form-urlencoded**, **form-data** (including real file fields), and **binary** (one local file as the whole body). Shipped at Postman parity plus GraphQL initially; GraphQL was later removed outright (not deprecated) and raw's five-language sub-selector was split into plain-text `raw` and a new top-level `code` mode, so the mode vocabulary no longer maps 1:1 onto Postman's own `body.mode` — a real translation is now P4's to build at the collection import/export boundary, not a given | Sequenced right after the core rather than folded into P2 because collections (P4) and curl generation (P5) both need to serialize/parse the *full* payload-format model — getting this right before those phases avoids revisiting their serialization once the format set changes |
| **P4 Collections: SQLite storage, real Postman-format import/export, left-panel tree** | **Storage lives in this app's existing SQLite database** (the same `kira.sqlite` connections/tabs/settings already live in), in whatever internally efficient representation the phase's own plan decides — a normalized folder/request schema, a serialized JSON column, or a mix — **not loose Postman-format files on disk**. There is no git-based, edit-the-file-directly workflow in this phase (no filesystem watching, no external-edit detection); that is explicitly deferred, not assumed away. What must be genuine, not approximate, is the **exchange format**: import reads a real Postman Collection v2.1 JSON file and reproduces its structure faithfully in SQLite, and export writes a real Postman Collection v2.1 JSON file back out from what's stored — so a collection round-trips through this app and remains a valid, faithful Postman collection on either side, even though the live, in-app representation is SQLite rather than the file itself. Surface collections in the left panel (built on P1's modularized panel) as a folder/request tree | The organizing structure the rest of the feature hangs off — history, curl, and the timeline all operate on a saved request, and a request needs somewhere to live first. SQLite keeps collections storage consistent with how every other piece of app state already persists (no new storage paradigm, no filesystem-sync problem to solve prematurely); the Postman-format fidelity is what import/export needs to actually be interoperable, and is a property of the conversion at the boundary rather than of the storage engine |
| **P5 Collection variables and environments** | Variables scoped to a collection, and separate named environments (a switchable set of variables layered on top of collection variables) — both resolved via `{{name}}` substitution wherever a request references one (URL, headers, body). Each variable/environment entry can be marked a **secret** with a checkbox; a secret's value is masked inline in the list and only shown after a system authentication prompt (reusing v1.1 P14's existing fingerprint/Touch ID reveal mechanism, not rebuilding it). Both variables and environments keep a **history of prior values**, not just the current one, and both support **reordering** | Sequenced right after collections (P4) since collection variables are scoped to a collection and need one to exist; sequenced before curl generation (now P7) because a generated curl command must substitute real values for any `{{variable}}` reference to be runnable, and before response history/raw inspector/timeline since none of those are meaningful until requests can actually use variables |
| **P6 Faker-backed dynamic values** | Postman/Bruno-style dynamic value generation inside a request — a `{{$...}}` variable form (random name, email, UUID, timestamp, and so on) resolved via `@faker-js/faker` (already a dependency, adopted in v1.1's P15 fake-data-generator phase) fresh on every send, rather than a stored value | Sequenced right after P5 because it extends the same `{{...}}` substitution engine P5 builds — a generator function resolved in place of a stored variable — rather than needing a second substitution mechanism |
| **P7 curl parse and generate** | Paste a curl command and have it populate a request (method, URL, headers, body, auth); generate an equivalent curl command from the current request's state, with any `{{variable}}`/`{{$dynamic}}` reference resolved to its real value in the generated command, since curl itself has no notion of either | Depends on P3's full payload-format model (a curl command's `-d`/`--data`/`-F` flags map onto exactly those body modes) and on P5/P6's substitution engine existing so a generated command is actually runnable as-is; benefits from P4 existing (a parsed curl command is naturally saved into a collection) |
| **P8 Response history** | Persist past responses per request (not just the most recent), browsable and comparable against each other | A response history entry is a saved snapshot of exactly what P2's response viewer already renders; sequenced after the core response viewer and after collections exist, since history is naturally scoped per saved request |
| **P9 Raw request/response inspector and raw request editor** | View the exact bytes sent and received for a request (not the structured/parsed model P2's builder presents), and author/edit a request at that raw level directly | Advanced-debugging capability that sits on top of the structured request/response model P2-P3 establish; needs a real request/response cycle to inspect, so it follows rather than precedes the core |
| **P10 Request timeline** | The full chronological sequence of what actually happened while a request ran — every step, not only the final response's own timing. Where the request involved one or more redirects, each hop is its own step with its own detail (method, URL, status, response headers), not folded away into a single number; within each hop, the phase breakdown (DNS resolution, connection, TLS handshake, wait/TTFB, content download) shown rather than one elapsed-time figure, with a reused connection shown as reused (no DNS/connect/TLS phases to report) rather than as an instant/zero one | `net/http/httptrace.ClientTrace` is the standard, low-risk mechanism for the phase data — hook callbacks on the request's context, not a parser or protocol implementation of our own — and since Go's built-in redirect-following client reuses the original request's context across every hop, one trace installed up front fires once per hop already; `internal/httpclient/client.go`'s existing `checkRedirect`/`[]RedirectHop` context-threading (added for P2/P3's own redirect-count/final-URL reporting) is the same bucketing technique this phase extends with per-hop timing, at the boundary each `checkRedirect` call marks. Needs real instrumentation of the HTTP client's own request lifecycle, which only exists once P2's core request path is built; sequenced after the raw inspector since both surface detail about the same underlying request execution |
| **P11 gRPC support** | Browse services/methods (via reflection or a supplied `.proto`) and issue unary and streaming gRPC calls, hosted through the same shell | The one non-HTTP protocol in scope for this chapter (WebSocket was dropped — see the chapter intro), schema-driven and streaming-capable, sequenced last among the feature phases so it lands on a shell and request/response/variables/history/timeline model already proven out by every phase before it |
| **P12 Studio/Http(→Api) modularization & separability audit** | Go back over every phase's output (P1-P11) and tighten the Studio/Http boundary described above: extract the Api-facing frontend code into its own genuine Bun workspace package(s) (`packages/api-core`, `packages/api-ui`, or similar — exact naming and split left to this phase's own plan; root `workspaces` gains `packages/*`) rather than just a tidier directory inside `apps/kira-studio`, matching the precedent v1.3's Git module sets; split any test file that grew to cover both modules' cases into one file per module (folder-separated, even where the runner command still runs both suites together); and audit `internal/bridge` and the remaining Go side for coupling that would block splitting Http into a standalone app later. **Also renames the module Http → Api**, visually (the mode tab and any other user-facing text naming the mode) and in code wherever an identifier/file/directory names the module rather than the HTTP protocol (`internal/httpclient` and other genuinely-protocol-shaped names are unaffected — see the module-boundary section above). Otherwise no new user-facing behavior — a structural pass, not a feature phase | Named up front as the phase every other phase should be minimizing work for, not a surprise cleanup at the end: doing this once, deliberately, after the module is feature-complete, is cheaper than either enforcing perfect separation through ten prior phases or leaving the coupling in place indefinitely. The rename belongs in the same pass since it touches the same files a separability audit is already moving, and "Api" is the honest name once gRPC (P11) makes the mode more than an HTTP client. The package split (rather than a directory-only move) matches v1.3's Git module so the two modules end up structurally consistent with each other, not just each internally tidy |
| **P13 Api module UI check and improvement** | A dedicated pass over every Api-mode surface (request builder, response viewer, collections tree, dialogs, empty/error states, and so on) checked against the rest of the app's own design system (`docs/design/kira-design-system`, `primitives.css`, the shared token scale) rather than each earlier phase's own ad hoc styling — brought consistent with Studio mode's spacing/type/color/icon conventions, tightened for practical, efficient use (dense enough for a power tool, not sparse for its own sake), and any leftover placeholder or rough-edge styling from earlier phases cleaned up. No new functionality — a visual/interaction-consistency pass, not a feature phase | Sequenced after modularization and the rename (P12) so it's polishing the module under its final name and directory shape rather than styling files P12 is about to move; sequenced before the code review (P14) so that review reads the UI in its finished, polished state rather than reviewing styling about to change again |
| **P14 Api module code review (2 rounds)** | Two rounds of dedicated code review scoped to the Api module only (renamed from Http in P12) — everything under its now-separated directories, plus its facing halves of `internal/httpclient`, `internal/postman`, and `internal/bridge` — the same discipline v1.1's P12 applied chapter-wide (`docs/v1.1/SPEC.md`), focused here on this chapter's own module | Sequenced last, after modularization, the rename, and the UI pass, so the review reads the module under its final name and in its final, separated, polished shape rather than reviewing code P12/P13 are about to move, rename, or restyle out from under it |
| **P15 (new) Request builder UX pass — part 1: layout, primitives and body mode** | A user-driven follow-up polish pass over the request builder's own surfaces, split from the editor-behaviour half below (see the two plan docs for the split's own rationale). Part 1 is the presentation half, eight of the twelve reported items: the response panel present — chrome and pane switcher included — from the moment a request tab opens rather than appearing only after a first send; a status code's meaning on its own always-visible line rather than a truncatable caption plus a hover; the Studio/Api mode-switch buttons sized up to the app's own tab metrics (and finally rendering the icon `modes.ts` has declared unused since P1); the headers/params/form-data tables' fields actually filling the cells they sit in (`.p-input` is `inline-flex`, so a `TextField` is ~20 characters wide wherever it is put unless its call site says otherwise — the same root cause as the URL field never growing); a new shared Checkbox primitive matching this app's own design system, replacing all fourteen raw `<input type="checkbox">` call sites app-wide (Studio included, since primitives already live in the shared theme layer — `AppButton`/`TextField`/`SegmentedControl` are precedent); JSON promoted to its own top-level body mode, as a UI-level promotion over unchanged `code`+`codeLanguage` storage rather than a new wire/Go/Postman vocabulary and a data migration; a body-present indicator on a request's tab, via a new optional `badge()` member on the tab-kind registry (which is the thing P4 D15 declined, answered rather than ignored); and the toolbar reflowed so Save sits with the request's name in the view head and the URL field takes the row | Sequenced after P12/P13 so it refines a surface that has already settled into its final module shape and design-system pass, rather than polishing files about to be renamed or restyled out from under it — the same role P13 played, but driven by concrete user-reported friction rather than a self-directed audit. Split from P15b because the two halves fail differently (a rule is present or absent here; a caret, a swallowed key or a stale decoration range there), because P15b opens with a `packages/api-core` foundation commit these eight fixes have nothing to do with, and because this half is the one that touches Studio — nine checkbox call sites and the title bar — so one review pass carries that risk instead of two. Plan: `plans/P15-request-builder-ux.md` |
| **P15b Request builder UX pass — part 2: input and editor behaviour** | The other four items, all landing in the same three files (`AutocompleteField.vue`, `CodeMirrorHost.vue`, `FieldRowsTable.vue`) and sharing one mechanism: header-name autocomplete over a curated well-known request-header vocabulary; `{{variable}}` references coloured distinctly for resolved / secret-deferred / unresolved, with a hover showing the resolved value (never a secret's) and autocomplete over known variable names, in the URL, header values and the body editor; auto-closing bracket/quote pairs; and arrow-key navigation across the row-based request tables | The `{{variable}}` work **extends** `AutocompleteField.vue`'s existing overlay trick (a real native `<input>` with a read-only CodeMirror painted behind it) rather than replacing these fields with editors — `page.fill()` across five spec files depends on them staying real inputs — with two additions the overlay cannot supply on its own: one `rangeHighlights` seam on `CodeMirrorHost` (colour that comes from *data*, not from a grammar) and one field-local hover panel on `theme/floatingPosition.ts`'s existing `pointReference` (the overlay is `pointer-events: none`, and the app's tooltip singleton anchors per element, not per token). The resolved value needs no new pipeline: `mergedValuesAndSecrets` + `resolve` already run per keystroke for the unresolved-count chip, and a secret's plaintext deliberately never reaches the renderer at all. Auto-close is enabled only where `editor/wrapSelection.ts`'s own reason for declining `closeBrackets()` does not apply (the console's unterminated-string lint), which is the request body editor and the plain fields. Plan: `plans/P15b-request-builder-editor-behavior.md`. **Implemented** — all seven commits (N1-N7) landed on `claude/feature-v1-2`; the plan's own checklist is checked off in full |
| **P16 (new) SQL grid polish, cross-cutting dropdown/input consistency, and Api search** | A second user-driven batch, mixing three genuinely different scopes rather than one surface: **(Studio only)** the SQL data view's pager/nav arrows repositioned closer to the grid's right edge, a real per-column minimum width so content stops being cropped, and the grid's elastic/overscroll behaviour made consistent on all four edges rather than only the bottom and right (either enabled everywhere or disabled everywhere); **(cross-cutting, Studio and Api both)** every dropdown control's closed-state height brought in line with the other controls it sits beside in the same row, and inline text inputs' crowding/padding and muted text contrast fixed at the design-system level, since both read as inconsistent app-wide rather than confined to one surface; **(Api)** a search affordance added to the request/response viewer, and to this module's other list-shaped surfaces (headers, variables, and similar) | The SQL-grid item is explicitly Studio-mode work, unlike every other phase in this chapter's own scope — captured here rather than opening a separate chapter for a handful of concrete, small fixes, the same way P15's shared-Checkbox item already crossed into Studio for a genuinely cross-cutting primitive. Sequenced after P15 per this repo's one-phase-at-a-time discipline, not because it depends on P15's own output — the two are largely independent, and this row exists mainly to record the request durably rather than let it live only in chat before its own plan is written. Plan: `plans/P16-sql-grid-consistency-search.md` — which resolves the row's own deliberate under-specification in three places: the pager moves into `ViewChrome`'s existing `#toolbar-end` group (last, after the pending-changes controls) rather than into a new slot, and the `RunState` reflow that would otherwise jitter it is fixed at the primitive; the per-column minimum becomes header-aware (the header cell spends 42-58px on padding, the 14px sort control and an optional PK/FK badge that no width computation accounted for), not a raised flat floor; and of the row's own "enabled everywhere or disabled everywhere", only *disabled* is expressible in CSS at all, so `overscroll-behavior: none` on every `.slick-viewport` is the branch taken — deliberately re-opening the earlier reverted "disable overscroll entirely" instruction, with a one-word revert path recorded. **Implemented** — all sixteen commits (Q1-Q16) plus one follow-up correction (a header-chrome constant `tests/ui`'s own new D4 case caught 8px short, found and fixed by that same test) landed on `claude/feature-v1-2`; the plan's own checklist is checked off in full. `bun run test:ui` passes 172/173, the one failure being `budgets.spec.ts`'s pre-existing cross-file-worker-contention flake (confirmed by re-running it alone, where it passes) |
| **P17 (new) Variable/environment management overhaul, Faker scoping, and value pipes** | A third user-driven batch, mostly real new capability rather than polish: the method select restyled to the app's own design system and colour-coded per HTTP method (Postman-style — GET/POST/PUT/DELETE/PATCH each a distinct colour); a Description field added to variable/environment entries (and any other configurable field this phase's own investigation finds it belongs on); editing a variable set or an environment opens as its own tab rather than a dialog; environments can be cloned; a bulk-edit mode for variables and environments through a `.env`-format text editor (paste/edit `KEY=VALUE` lines, applied as a diff against the existing set — a real parse/serialize/reconcile design, not just a textarea); the Faker dynamic-value catalogue gains autocomplete and is re-scoped under a `fake.`-style namespace rather than today's flat `$name` list (exact shape left to this phase's own plan); a pipe/transform syntax for variables, environments, and dynamic values alike — starting with base64 encode/decode, upper/lower case, and URL encode/decode; and a single panel, reachable from any request tab, showing the active environment's and the collection's variables together rather than through two separate dialogs | Sequenced after P15/P15b/P16 per this repo's one-phase-at-a-time discipline — recorded here so the request survives durably rather than living only in chat before its own plan exists. **The pipe/transform item is this phase's real risk and needs its own care, not a quick bolt-on**: `substitute.ts`'s own doc comment states "no expression language" as a deliberate, load-bearing design choice (D1/D17) — P5's secret-masking discipline, P6's per-occurrence dynamic-value freshness, and P9/P10/P14's hard-won secret-masking-at-every-encoding-surface fixes are all built on the substitution grammar staying a single, simple, one-pass find-`{{`-find-`}}` scan. Introducing pipes is a genuine, deliberate reopening of that decision — this phase's own plan must work out, explicitly, where a piped/transformed secret's masking replacer needs to register the *transformed* form (the same class of gap P14 round 2 found and fixed for encoded secrets — a base64-encoded secret is exactly as sensitive as its plaintext, and a naive implementation would silently reopen that exact bug class), and whether a pipe applies before or after per-occurrence dynamic-value generation. Plan: `plans/P17-variable-environment-overhaul.md` — which answers the row's three named questions and resolves its deliberate under-specification in four places: the pipe reopens the *name read* (`text.slice(open+2, close).trim()`) and **not** the `{{`/`}}` scan, with an all-or-nothing "every segment after the first must be a known transform, else the whole text is the name" parse that makes the grammar change provably inert for every input that exists today (the pinned Go/TS corpus passing unedited is the guard); `apivars.Resolver` stops recording `name → plaintext` and starts recording *the exact text it wrote* per distinct `(name, pipeline)` pair — so `secretReplacer` masks the base64/upper/URL-encoded form, one entry per spelling (a request using both `{{secret}}` and `{{secret \| base64}}` masks both, which a name-keyed map structurally cannot), with the rendered form reaching the resolver through a callback rather than a field on `Reference` (a struct whose purpose is to be reported); a pipe applies **after** per-occurrence dynamic-value generation, leaving P6 D3's freshness and D7's short-circuit untouched by construction; and the Faker re-scoping is **additive and permanent, with no migration at all** — `fake.<faker module>.<method>` (the vocabulary Studio's own `fakeData/recipes.ts` already uses) beside Postman's `$name` spellings, which stay because they are the import/export contract with Postman, not a legacy spelling this app is free to retire. **Implemented** — all fourteen commits (R1-R14) landed on `claude/feature-v1-2`; the plan's own checklist is checked off in full. R3, the security commit, replaces `Resolver.Used()`'s plaintext-keyed map with `UsedSecret{Name, Rendered, Placeholder}` fed through an `onResolved` callback, masks every rendered/piped form via a length-sorted `strings.Replacer`, and is proven by dedicated Go tests plus `tests/ui/http-pipes.spec.ts`'s own three end-to-end cases (a piped secret never resolves client-side; the Raw pane/Timeline hop/persisted history entry all show the masked reference; both spellings of one secret mask as one distinct secret). `bun run test:ui` passes 183/184, the one failure being `budgets.spec.ts`'s pre-existing cross-file-worker-contention flake (confirmed by re-running it alone, where it passes) |
| **P18 (new) History correctness, retention, gRPC parity, mode-button alignment, and environment colour** | A fourth user-driven batch, five items: (1) the response-history list does not refresh as new requests are sent — a real live-update bug in whatever store/watch wiring `packages/api-core`'s history module and its consuming panel share, not a display-only issue; (2) history currently grows unbounded — cap it at 30 entries per request (oldest evicted first), keep storing the full raw request/response alongside each entry (response bodies are already persisted up to `maxHistoryBodyBytes` in `internal/storage/repos/response_history.go`; this phase's own investigation must confirm the *request* side and gRPC's `grpc_history.go` get the same treatment) and show an explicit "only the last 30 are kept" message at the bottom of a history list that has been truncated; (3) a parity pass applying P15/P15b's request-builder UX and editor-behaviour fixes to the gRPC request view (`GrpcRequestView.vue`), which shares the same table/editor primitives but was not itself in either phase's file list; (4) the Studio/Api mode-switch buttons' icon and label are misaligned — a regression or gap in P15's own icon-rendering fix (`workbench/TitleBar.vue`); (5) environments get the same colour-coding treatment Studio already gives DB connections, shown wherever the active environment is surfaced in the request panel, so a request's environment is visually identifiable the same way a query console's connection already is | Sequenced after P17 per this repo's one-phase-at-a-time discipline — recorded here so the batch survives durably rather than living only in chat before its own plan exists. Items 1 and 4 are regressions/gaps against this chapter's own recent work (history from P8, the mode buttons from P15) and should be root-caused against what actually changed, not treated as fresh features; item 5 is a deliberate cross-reference to Studio's existing connection-colour convention (v1.1 P24/P27 territory) rather than a new colour scheme invented from scratch. Plan: `plans/P18-history-grpc-parity-mode-buttons-env-colour.md` — which corrects three of this row's own premises against the real tree, and its corrections change what gets built: the history store is **not** in `packages/api-core` (it is `api/state/history.ts`, P12 D12's shared factory); history is **not** unbounded (both tables have carried a 20-per-scope cap since P8 D6/P11 D11, so item 2 is a raise to 30 plus a name the renderer can read, not new eviction machinery); and the investigation the row asks for found the request side genuinely broken in two places — HTTP stores the request and its truncation flag but **nothing has ever rendered either**, and gRPC stores its request message with **no per-entry cap at all**, which silently breaks the invariant its global byte sweep depends on (one oversized message empties `grpc_call_history` entirely). Item 1's root cause is single and provable: `createHistoryStore`'s lazy branch sets `stale` and **no commit in this repository has ever read that flag** — so a send with the History pane closed leaves the list frozen for good; item 3 resolves into five real gaps, four fixes gRPC already inherited, and three that deliberately do not apply; item 4 is quantified from the icon font itself (an unboxed icon makes the icon→label gap differ between the two tabs by ~1.6px, against the app's own "icons never float unboxed next to text" law) and made measurable rather than re-guessed, since P15 §4 explicitly declined a real render; item 5 follows LAW 07 exactly — one palette, user-assigned, `none` by default, surfaced as a rail and a dot and nowhere else. **Implemented** — all sixteen commits (S1-S16) landed on `claude/feature-v1-2`; the plan's own checklist is checked off in full. S1/S2 close item 1 at the root: `ensureLoaded` becomes `ensureFresh` with `stale` as a second precondition, and `ensure()` always hands back the reactive proxy, guarded by a new `history-runtime-reactivity.spec.ts` (fails on either pre-fix line) and a `http-history.spec.ts`/`grpc-request.spec.ts` case that reproduces the exact user-visible repro end to end (fails with 0 rows on `c4bb908`). S3-S6 raise both retention caps to 30 (`HISTORY_PER_SCOPE_LIMIT`/`GRPC_HISTORY_PER_SCOPE_LIMIT`, pinned equal to the Go constants by a parity test), give gRPC's request message the 256 KiB cap it never had (closing the table-emptying hazard the global byte sweep depended on — proven by a Go test that empties the table without the cap and survives with it), and add the "only the last 30 are kept" note. S7-S10 close item 3's five real gaps: `{{variable}}` colouring/hover/completion (via `variableCompletion.ts`'s move to `api/state/`) and a `grpc-timeout`-style metadata vocabulary reach the gRPC view, the response pane is a panel from tab-open, `grpcCodeHint()` gives non-OK codes their own meaning line, and Save/the target field take P15 D7/D4's exact shape. S11 boxes the mode tab's icon and labels its text, closing the measured ~1.6px gap discrepancy with a new geometric test (times out on the pre-fix markup). S12-S14 give environments a user-assigned colour from Studio's own palette (`ColorPicker` promoted to `theme/primitives/`, `domain/color.ts` the shared home), surfaced via an app-drawn `EnvironmentSelect`, both request views' toolbar rail and head dot, the environment's own tab, the manage dialog, and the variables overview panel — deliberately excluding history rows and the tab strip. S15 gives the stored request side a reader at last (`RawExchangePane.vue` reconstructs both documents from the four stage-1 fields a snapshot carries, via a new `generateRawRequestFromStored` adapter), closing the one gap F8 found with no new storage. `go build`/`go vet`/`go test ./internal/...` all clean; `bun run test:ui` passes 197/198, the one failure being `budgets.spec.ts`'s pre-existing cross-file-worker-contention flake (confirmed by re-running it alone, where it passes) |
| **P19 (new) Connection dialog sizing, Mongo console fixes, query-result selection, and SQL tooling regression** | A fifth user-driven batch, Studio-mode this time (like P16's SQL-grid item, kept in this chapter rather than opening a separate one): (1) the Add Connection dialog changes size as the user switches between tabs and sub-tabs — pin it to one static size regardless of which tab/sub-tab is active; (2) Mongo's query console gains a "copy as json" action for a query result, and a real bug where the console's filter input renders and behaves as inert placeholder text while the adjacent order/sort input actually applies — the filter needs to reach parity with the order input's own wiring; (3) query console results become selectable — rows, columns, or an arbitrary free-form cell range — so a selection can be copied as multiple values, not just one cell at a time; (4) the SQL formatter (v1.1 P13's Format button) and the SQL table/field-name autocomplete are both reported broken — root-cause each against what P13/P18 originally shipped rather than assuming a shared cause; (5) v1.1 P27's active-filter/order-by indicator colour is blue in the surfaces where the query text itself is also syntax-highlighted blue, making "a filter is active" indistinguishable from "this is part of the query" — pick a different indicator colour that reads as a distinct state, not the query language's own highlight colour | Sequenced after P18 per this repo's one-phase-at-a-time discipline. Kept in this chapter's own SPEC rather than v1.1's (that chapter's already closed out through P29) so every open user-reported item lives in one list. Items 4 and 5 are regressions/gaps against v1.1's own shipped work (P13/P18's SQL tooling, P27's indicator colour) and should be root-caused against what actually changed rather than treated as fresh features. Plan: `plans/P19-connection-dialog-mongo-console-sql-tooling.md` — which corrects four of this row's own premises against the real tree, and every correction changes what gets built: the filter/order pair item 2 describes is **not** in the query console (v1.1 P27 §1.4 already recorded that the console has no filter or order input at all) but in the Mongo **collection** view, `views/documents/DocumentView.vue`; that filter's "renders **and behaves** as inert" is half right — the render half has one exact cause (the box is the only filter surface in the app with no `prefix` label at all, which v1.1 P27 §1.2 deliberately skipped, and its typed value is painted through the syntax overlay in the same greys and pale blues as chrome), while the behave half is not reproducible (`tests/ui/autocomplete.spec.ts` has been applying a real filter through that exact input and asserting the row count drops since P18), so item 2's real behaviour work is three named wiring gaps against `FilterToolbar.vue` — no no-op guard on blur (every idle focus-out repages the collection and discards the exact count), no `@escape` revert, no resync watcher; the SQL **formatter** is not broken (`sql-formatter@15.8.2` was run in the worktree over a twenty-case battery of realistic statements — every one this app can reach formats correctly; the chunk/Rolldown, command-id-collision and `setText` theories are each checked and excluded), and what actually makes Format read as broken is three things P13's own plan wrote down and deferred: its §3's declined statement-by-statement alternative (one unparseable fragment silently takes the whole press down — reproduced), its **OQ-2** (the caret jumps to offset 0, which also re-points *Run statement* at statement 1 — still open, and predating P13 rather than regressed into it), and D4's `keywordCase: 'preserve'` making an already-formatted document a silent no-op; and the SQL **autocomplete** is not a regression at all — table/column completion is fed *exclusively* by a DDL document the user must hand-paste into a dialog reachable only from a connection row's context menu, so with no such document (every connection by default) `sqlCompletionSources` returns `undefined` **by design**, pinned by `sql-schema.spec.ts`'s own "with no DDL document, the console is unchanged" case, with the parser itself proven fine against a real `pg_dump` and a `SHOW CREATE TABLE` in the worktree. The fixes follow from those corrections: one static 620×520 dialog for both steps and every tab/sub-tab (pinned by a geometry test that also proves no pane scrolls, since the constant is derived arithmetic rather than a measurement this sandbox can take); a `FILTER` label plus the three missing behaviours on both Mongo boxes; the data grid's own `SlickHybridSelectionModel` ported to the console's tabular result — with `clipboardFormats.ts` and `slick/selection.ts` **promoted to `views/shared/` because `biome.json` makes `views/console/**` importing `views/grid/**` a lint error**, not as a matter of taste — plus ⌘C and a context menu speaking `views/grid/menu.ts`'s exact vocabulary; per-statement formatting that emits an unformattable statement verbatim and names it in a warn strip; a caret that stays in the statement it was in (closing P13 OQ-2 behind one default-off `CodeMirrorHost` prop, so the four other hosts that prop was deferred over stay byte-for-byte unchanged); two new supplies for the language service that leave its DDL-only architecture intact — relation names read straight from the project tree's existing cache (no round trip, exactly what Mongo's own completion has done since v1.1) and a "Fill from connection" action in the Schema (DDL) dialog that stages `TreeService.Definition`'s output, which is already precisely the `CREATE TABLE`/`ALTER TABLE … ADD CONSTRAINT`/`COMMENT ON COLUMN` vocabulary `parseDdl` consumes; and one new `--kira-state-on: #4ec9b0` token at ~168° — the one hue region no syntax token and no other state colour occupies — replacing `--kira-accent` (#0078d4, within 8° of the `#569cd6`/`#9cdcfe` the same input box paints its own query text in) for the whole "a filter/sort/projection is applied" family. **Implemented** — fifteen commits landed on `claude/feature-v1-2` (the plan's own T6/T7 pair merged into one, since both live entirely inside `ConsoleSlickGrid.vue`'s shared selection state and splitting the diff would have meant reconstructing an intermediate, less-useful state purely for commit count; every §4.3 test case is instead distributed into its own feature's commit rather than gathered into a separate T17, which is what the plan's own commit-by-commit verification cadence already implies). Item 1: `ConnectionDialog.vue` is one static 620×520 for both steps, pinned by a new geometry test in `connection-dialog-tabs.spec.ts` that records the dialog's box across every step/tab/sub-tab/engine-family combination and asserts it never moves, plus that no pane's `scrollHeight` ever exceeds its `clientHeight` — the 520 arithmetic held on the first try. Item 2: `DocumentView.vue`'s filter box gets the `FILTER` label (`prefix-active` reading `tab.state.search`) and the three wiring gaps `FilterToolbar.vue` already had — a no-op guard on blur, `@escape` revert, a resync watcher — verified end to end against a real `stream.ops()` count (an idle blur issues zero new `data:read`/`data:count` calls and the exact count survives it). Item 3: `views/shared/clipboardFormats.ts` and `views/shared/slick/selection.ts` (mandatory promotions, `biome.json`'s `views/console/**`-importing-`views/grid/**` lint ban) back a real `SlickHybridSelectionModel` on the console's tabular result — row/column/range selection, ⌘C and a context menu (`views/console/resultMenu.ts`) per kind, mirroring `views/grid/menu.ts`'s vocabulary — plus a document/key-value row menu ("Copy as JSON"/"Copy all as JSON") and the same item on the Mongo collection view's own row menu. Item 4: `formatConsoleText` is now per-statement (`{text, ok, failures}`), so one unparseable fragment (`\dt` after a good `SELECT`) no longer blocks its neighbours — a warn strip names which statement, a note strip says "Already formatted." for the byte-identical no-op, and the caret survives a reformat by statement index (`CodeMirrorHost`'s new opt-in `keepSelectionOnExternalSync`/`setCursor`, every other host unchanged), closing v1.1 P13's own OQ-2; separately, `sqlCompletionSources` is now two layers (a DDL document still wins when one exists; with none, table names come from the tree's own cache via a new `consoleRelationNames`, mongoCollectionNames' identical technique) and `SchemaDialog.vue` gained "Fill from connection" (`state/schemas.ts`'s `connectionRelationsFromTree`/`fillDdlFromConnection`, staging `TreeService.Definition`'s real output — never auto-saving) plus a dismissible per-connection "no schema" hint in the console. Item 5: `--kira-state-on` replaces `--kira-accent` on `.ph-active` and, as a separate commit, on the two other sibling indicators that actually used it (`IconButton`'s `.has-indicator` dot, `StreamView`'s partition button) — the plan's own F28 additionally named the SlickGrid sort chevron as a third sibling, but reading the actual rule at this phase's base commit shows it had already moved to a deliberately neutral `--kira-fg`/`--kira-fg-muted` by a later migration pass, with its own "Neutral, not accent" comment in place; recolouring it to match a stale comment elsewhere in the same file would have undone that prior decision for no benefit to the reported bug, so only the stale comment was fixed. Validation: `bun run lint`/`typecheck` clean; `go build`/`go vet`/`go test ./apps/kira-studio/internal/...` all clean (no Go files touched this phase); `bun run test:unit` 483/483; `bun run test:ipc:fe` 7/7; `bun run test:ui` 207/208, the one failure being `budgets.spec.ts`'s own documented cross-file-worker-contention flake (confirmed passing alone, p50 7ms against its 12ms budget) |
| **P20 (new) README feature overview** | Rewrite `README.md` to actually describe what the app does today — a short, feature-by-feature summary (Studio's per-database consoles/grids/fake-data-generation/etc., Api's request builder/collections/environments/gRPC/etc.) in plain, brief mentions, not implementation detail. The point of this pass is presentation — what the app *does* — not a development or architecture doc; `docs/ARCHITECTURE.md` stays the source of truth for how it's built | The README had never had a dedicated pass to catch up with everything v1.1 and this chapter have actually shipped, and a prospective user/reader had nowhere short to look to find out what the app does. **Implemented** — the intro line and Status section now name both workbenches (Studio shipped through v1.1; Api in development as this chapter) instead of describing a single-purpose v1.1-era db client; the existing feature list was retitled "Studio features" unchanged, and a new "Api features" section was added alongside it (request builder incl. all real body modes — raw, syntax-highlighted code, form-urlencoded, multipart form-data, binary file; collections; environments/variables with colour and Faker; curl import/export; the 30-entry response history cap; request timeline; gRPC), each item checked directly against the frontend source rather than assumed from the SPEC's own phase titles — one drafted claim (a dedicated Basic/Bearer/API-key/OAuth2 auth UI) was found to not exist in the codebase (auth is set via the Headers panel, matching curl parity) and was corrected before commit. The Documentation and top-level-layout sections' doc links were also updated to point at `docs/v1.2/` as the live chapter, alongside the existing `docs/v1.1/` reference for the completed one |
| **P21 (new) Code review, three rounds — whole application** | `AGENTS.md`'s code-review process (three parallel analysis agents per round — architecture/structure/maintainability/security, functional correctness/business logic, performance/resource efficiency — findings only, then one sequential agent fixes everything found), for three full rounds. **Scope is the entire application as it stands, Studio and Api both, not a diff against any one phase or batch of phases** — every phase up to and including P20 is in scope, and this is a from-scratch review of the current tree rather than a review of what changed recently. Each round's analysis agents run on Opus; the fix pass runs on Sonnet, matching this chapter's own P14 convention. **Round 1 is complete.** The three Opus analysis agents each found ~10 issues (architecture/security, functional correctness, performance — roughly 30 in total); the Sonnet fix pass landed essentially all of them, one commit per logical fix. Security/architecture: MySQL/MariaDB URI query parameters no longer splice into a connect-time `SET` statement; `sslmode=require` now verifies the server certificate on Redis/MongoDB (matching Kafka's own behaviour, with an explicit `verify-none` escape hatch) and `rediss://` finally enables TLS; an imported Postman collection's file reference is never carried into a live, sendable path; response headers are masked for secrets the same as the request half; a revealed variable re-masks itself at the auth grace window instead of staying legible until the tab closes; `internal/ipcerr` moved out from under `internal/bridge` with a `go list -deps` test guarding the layering; a pre-connect script's fallback `PATH` no longer leads with an empty (`.`) element; `docs/ARCHITECTURE.md`/README/PACKAGING named the database file `kira.sqlite` where the tree has called it `kira.db` since the Go port; a failed collection import now rolls back the collection it partially created; `postman.Parse`'s whole-file read is bounded; a panicked `*adapters.Error`'s own code survives `safeRun`'s recover() instead of always folding to `E_INTERNAL`; and `StreamView.vue`'s compose dialog and Delete button read `Caps` instead of `connection.kind`. Functional correctness: the column projection can no longer be driven to zero columns from any of three menu paths; Duplicate row(s) no longer stages a truncated cell as real data and Commit fails loudly instead of silently no-opping on an unaddressable row; generated SQL literals are dialect-aware (backslash escaping); the `` {{name \| transform}} `` pipe vocabulary is now byte-identical between Go and TypeScript (base64/urldecode/case-mapping); a server-streaming gRPC call's terminal event can no longer be dropped by a network-vs-event-channel race; MariaDB's fake-data generator respects signed integer display widths and mysql/ClickHouse temporal formats; the response-history stale flag is sequenced against a concurrent send; both history tables cap the whole snapshot rather than field by field; `applyLoadFailure` sets a real status on disconnect for every view, not just console; and dollar-quoting in the SQL splitter/linter is Postgres-only. Performance: the row context menu builds its clipboard snapshots lazily instead of eagerly (a Delete keystroke on a large selection went from seconds to milliseconds); `resolveColumnOrder` is no longer O(cols²) and is memoized; the console's document parse cache is pruned to the rendered window; both history repos' byte-budget sweep is gated on a cheap aggregate instead of running unconditionally; an oversized page is rejected by its own measured size before being encoded; the project tree row builder no longer spreads into `Array.push` (a hard failure past ~65k rows); and two small per-frame DOM/allocation costs on the grid's scroll path were removed. One fix (a blanket primary-key lock on Hide column, matching ColumnsMenu.vue's own checkbox too literally) broke tested composite-primary-key behaviour and was caught and corrected by the full UI suite, not by the review itself, before landing. Explicitly skipped, with reasons recorded in the fix-pass session rather than repeated here: `internal/apivars`/`bridge/grpc.go`'s duplicated protocol-neutral field-walk (a structural refactor with no active bug, judged too large relative to its benefit for this pass); the `LiteralRenderer` "dialect-aware preview" item (already handled correctly for MySQL/MariaDB/ClickHouse by each adapter's own existing override — the finding predates those); the response-history "only the last 30 are kept" note and nested-block-comment splitting edge case (both real but narrow); and two performance findings (`DocumentView`'s per-toggle full-page height recompute, and `ConsoleResultGrid`'s full materialization of every document row) that got a partial fix (the missing cache-pruning call) with the deeper structural change left for a later pass. Validation: `bun run lint`/`typecheck` clean, `go build`/`vet ./...` clean, `go test -p 1 ./apps/kira-studio/internal/...` all pass, `bun run test:unit` (537 tests) and `test:ipc:fe` (7 tests) all pass, and the full `test:ui` suite passes except the one documented pre-existing flake (`budgets.spec.ts`'s scroll p50 threshold, confirmed passing in isolation both times it appeared). **Round 2 is complete.** A fresh, from-scratch pass over the round-1-fixed tree; the three Opus analysis agents found roughly 28 further issues, and the Sonnet fix pass landed all but two (one documented as a deliberate decision, one left as a partial fix with reasoning recorded), again one commit per logical fix, validated and pushed after a container restart interrupted and then had to be recovered mid-pass. Security/architecture: pasting a curl command with `-F name=@path`, a lone `--data-binary @path`, or `-T path` no longer resolves a live, sendable local file path (the same exfiltration vector round 1 closed for Postman import, reopened on curl's own importer); `connections.Update` now reconnects a live connection when its destination or `ReadOnly` flag actually changes, instead of leaving the old adapter, cache and server-side enforcement pointed at the old destination; three separate `sslmode` vocabulary gaps closed (Kafka's own documented `verify-none`/`insecure` escape hatch was rejected outright, ClickHouse rejected `prefer`, Postgres rejected `verify-ca`) with the remaining "six adapters, six vocabularies" duplication and libpq's `allow` semantics recorded as a real but separate refactor; a successful (not just failed) gRPC call's `StatusMessage`/headers/trailers are masked before either the renderer or `kira.db` ever sees them; all three transient revealed-secret maps (variables, history popover, Copy-as-curl) now re-mask on the same 5-minute grace expiry, not only on their own dialog's close; MySQL/MariaDB's TLS `ServerName` now uses `net.SplitHostPort` (fixing IPv6 hosts) and its process-global TLS config registry no longer leaks one entry per connection id; two concurrent "Test connection" presses across multi-window no longer share one Preconnect key and can no longer kill each other's script. Left open: `internal/storage/model`'s reuse of `internal/httpclient` types (the layering *test* was fixed to enumerate every `internal/*` package instead of a hand-maintained list, but the underlying coupling itself is a real, separate refactor); `kira:v2` ciphertext binding no additional authenticated data to its row is now documented as an accepted design constraint rather than fixed, since closing it would break `SecretStore.copy()`'s duplication path. Functional: SQS's own headline "never polls automatically" guarantee, broken on three call paths (an immediate mutation's self-reload, its sibling-tab fan-out, and a project-tree double-click reuse), all now funnel through one guarded `reload()`; hiding one column of a composite primary key now fails loudly naming the hidden column instead of reaching an opaque server-side refusal; `DocumentView`/`StreamView` now gate writes on the connection's `ReadOnly` flag as well as adapter `Caps`, matching the grid/keyvalue/browse views; deleting a folder or collection now purges every cascade-deleted descendant's cached request, not just the row directly deleted, fixing a Save button that looked live on an orphaned tab; Copy-as-curl now emits a form-data text row's own `Content-Type`; a streaming gRPC call's history row is now recorded before its terminal event fires, closing the write-side twin of round 1's read-side race; a stream/Kafka view's "N total" is now scoped to the active partition/offset/timestamp filter instead of the whole topic; a failed `tabsSave` no longer permanently blocks a later, identical-looking save. Performance: the stream find box no longer decodes and caches an entire page or allocates a combined per-row haystack on the first search keystroke; console/documents row context menus build their clipboard/JSON snapshots lazily, mirroring round 1's grid fix; MySQL/MariaDB connections now serialize concurrent ops against the same pinned connection, matching Postgres's existing fix; Postgres's own `ConnSet` no longer blocks every operation during LRU eviction and no longer races two concurrent dials of the same cold database; the pre-connect stderr tail tracker no longer grows without bound on newline-free output; the console's statement-at-cursor split no longer reruns on a bare caret move, only on an actual keystroke; the search toolbar no longer copies its whole match array every scan tick and no longer rebuilds its match index on a bare Next/Prev; the gRPC live-stream buffer no longer deep-reactivates every incoming message (`markRaw`); the HTTP response pane's pretty-printed body is retained only while Pretty view is actually shown; a metrics sampler tick no longer probes every process twice; a stream view's column resize no longer writes tab state on every `pointermove`, only on release. One fix in this pass (trimming the gRPC live-message buffer to 90% of its cap instead of back to the cap itself, to amortize splice cost) broke D15/D17's own "keeps exactly the most recent 10,000" contract, caught by the existing `grpc-request.spec.ts` UI test; reverted to the exact-cap trim (the accompanying `markRaw` fix, which was never in tension with the contract, stands). The HTTP response-body-retention fix and the search/DocumentView match-index memoization above are each partial per their own commit messages, with the remaining, larger piece of each recorded there rather than folded in. Validation: `bun run lint`/`typecheck` clean, `go build`/`vet ./...` clean, `go test -p 1 ./apps/kira-studio/internal/...` all pass, `bun run test:unit` and `test:ipc:fe` all pass, and the full `test:ui` suite passes except the one documented pre-existing flake. **Round 3 is complete.** A third fresh, from-scratch pass over the round-2-fixed tree; the three Opus analysis agents found 11 architecture/security, 16 functional and 13 performance issues, and the Sonnet fix pass landed all but three (each a deliberate, recorded skip), one commit per logical fix, plus one further commit correcting a real regression the fix pass introduced in itself and caught via the full `test:ui` run before pushing. Security/architecture: `variables.Upsert` now purges a variable's plaintext-value history the moment it transitions to a secret, instead of leaving every prior plaintext revision readable forever through the history endpoint; `oplog`'s `Stop()` now blocks until its own consumer goroutine has actually exited, closing a shutdown race; `connections.Update`/`Insert` route a changed secret through the store the same transactional way a fresh connection does, so a rotated password can no longer be found sitting under the connection's old, superseded field; `httpclient`'s redirect handling strips caller-supplied headers (auth tokens included) before following a cross-host redirect; the pre-connect supervisor's kill path no longer race-signals a process whose pid the OS may already have recycled; a collection export now writes its file atomically, so a failed write can no longer leave a half-written file in place of the original; `adapterhost`'s response path no longer needs a nil-log guard at every call site, folded into one `withDefaultLog` seam instead; Postman export strips a local absolute file path down to its base name rather than round-tripping it into the exported collection; SQLite's own DSN parsing rejects URI metacharacters that could otherwise smuggle extra `file:` query parameters past the app's own validation; and grid/console clipboard TSV export neutralizes a leading formula-trigger character (`=`, `+`, `-`, `@`, tab, CR) the same way CSV export already did, closing the CWE-1236 spreadsheet-injection gap for the TSV path specifically. Left open: the `Tree`/`Connections`/`Collections` services' inconsistent `context.Context` threading (a real structural gap, but a large, invasive refactor across every call site with no single active bug behind it — judged too large for this pass). Functional: the fake-data generator's `finance.amount`/`binary.hex` (and every other generator, via a universal bounds clamp) can no longer emit a value outside its own declared column bounds; Kafka's paginated read now terminates on a page that lands exactly on a run of empty polls, instead of spinning forever; a superseded/failed `history.ts` load now self-retries instead of leaving the panel stuck on stale data; Mongo's sort-document editor now round-trips a key exactly as typed instead of silently reformatting it; the curl importer/generator now round-trips `--data-urlencode` verbatim and extends the existing `;`-in-value guard to `-F`; the timestamp cell editor's two-digit-year path now anchors against a real placeholder year and writes back a zero-padded four-digit one instead of silently producing a two-digit year string; the URL query codec now treats a bare `+` and an escaped `%2B` symmetrically instead of losing the distinction on the round trip; the pager's Jump-to-page control now resyncs and clamps against the page count that was actually current when the jump landed, not a stale one; collapsing all documents on one page no longer wipes the recorded expansion state of every other loaded page; the browse view's `setLevel` now clears the previous level's filter/selection on descend, and a failed load clears the stale node list instead of leaving it rendered under the new breadcrumb; ClickHouse's `computeOrderBySql` now falls back to a deterministic all-columns `ORDER BY` instead of an unstable one when no better ordering is available; and `enginecache`'s stale-flip path now uses a dedicated `ByteLru.Update()` instead of a Set-based re-insert that silently lost the entry's place in LRU eviction order (the finding's own claim about which internal clock this affected was independently tested, found inaccurate, and corrected rather than shipped as stated). Performance: the paged find box now budgets its per-frame scan by cell count instead of a flat row count (a 60-column table no longer does 120,000 decode+regex pairs in one animation frame) and debounces its query input at the same 150 ms the tree/collections search boxes already use; the HTTP response find bar's `findRanges` is now memoized by `(doc, query)`, cutting four to five full-document lowercase-and-scan passes per keystroke down to at most one per distinct document; a document row's visible-line count is now cached independently of the parse-cache window that `pruneRows` evicts on every scroll, so toggling one row's expansion no longer re-parses every other expanded document on the page; MySQL/MariaDB's and Redis's connection sets now carry the same dial single-flight and lock-free LRU-eviction split Postgres's own round-2 fix already has, closing a leaked-connection race and a same-connection head-of-line block in both; the browse view's loaded node list is now `markRaw`'d instead of deep-reactive, matching the project tree's own precedent for a list that can hold up to 200,000 entries; tab persistence sends its already-built change-check snapshot (parsed back to a plain object) instead of re-serializing the live reactive tab tree a second time, and `TabsRepo.Save` now upserts/prunes instead of unconditionally deleting and reinserting every row for the window on every save. One fix in this pass (SlickGrid's per-cell staged-edit extractor, hoisting its `pendingChanges` read to build time on the mistaken belief the extractor is always rebuilt on a staging change) broke staged-edit rendering outright — caught by the full `test:ui` run (`mutations.spec.ts`, `cell-editor.spec.ts`, `interaction.spec.ts` all failing), root-caused, and corrected in a follow-up commit that reads the same state per cell instead, keeping three of the four proxy-trap removals the original finding wanted while restoring correctness. Explicitly skipped, with reasons recorded in the fix-pass session rather than repeated here: `rangeHighlightPlugin`'s full-document rescan on every keystroke (a CodeMirror `ViewPlugin` with no unit-test seam and a real correctness risk around viewport-boundary widening, judged too risky for this pass on its own — findRanges' own memoization fix already removes most of the redundant work it triggers); the browse view's filter-input debounce and lowercase-name precompute (the remaining half of the browse-view finding — the code change is small, but `BrowseView.vue` has no existing Playwright coverage at all to verify a `<script setup>` refactor against, and writing a new end-to-end fixture was judged out of scope); and the gRPC response pane's per-batch row-height remap while a message is expanded (bounded to `VirtualList.vue`, a component shared across grid/documents/browse/console/gRPC, where the finding itself notes lower severity and a real correctness risk from touching a component with that much blast radius). Validation: `bun run lint`/`typecheck` clean, `go build`/`vet ./...` clean, `go test -p 1 ./apps/kira-studio/internal/...` all pass (including the real testcontainers-backed MySQL/MariaDB/Redis suites the connection-set fixes touch), `bun run test:unit` and `test:ipc:fe` all pass, and the full `test:ui` suite passes except the one documented pre-existing flake (`budgets.spec.ts`'s scroll p50 threshold, confirmed passing in isolation). | This chapter has had two prior module-scoped reviews (P14, scoped to the Api module only) but never a whole-application pass covering Studio and Api together — the same discipline v1.1's P2/P12 applied chapter-wide for that chapter. Sequenced last, after every other phase in this list, so the review reads the app in its final shipped state rather than reviewing code a later phase is about to change out from under it |
| **P22 (new) Sixth user-driven batch — Api/Studio polish, global sizing tokens, search sweep, and schema-aware SQL tooling** | A large, mixed batch of concrete user-reported items, grouped by theme rather than sequence (this phase's own plan may re-split into sub-phases, e.g. P22b, the way P15/P15b did): **(status/response)** the response status code's meaning currently shows only on hover — make it visible by default (re-check against P15 §2/P18 S9, which each already claimed this for HTTP/gRPC respectively, since the user is reporting it as still hover-only); **(headers)** header **value** autocomplete, not just the existing name autocomplete — full canned values for headers like `Content-Type`, and a prefix-only vocabulary (`Bearer`, `Basic`, `Digest`, and so on) for `Authorization`; **(request builder)** the Save button right-aligned; **(variables)** `{{variable}}` references get a visually distinct colour for "resolves" vs. "does not resolve" (re-check against P15b's existing resolved/secret-deferred/unresolved colouring — the user wants a clear correct/incorrect binary), a value shown on hover, and autocomplete over known names (also re-check against P15b, which already built exactly this); **(Studio SQL grid)** the pager/nav arrows moved closer to the grid's right edge (re-check against P16, which already claimed this); **(global sizing)** the page-number input (and any other control in the same family) is visibly taller than the controls beside it — fix the immediate inconsistency and, structurally, expose control height as a small set of global CSS custom properties so that raising one token raises inputs, dropdowns, and other form controls together instead of each needing its own fix; **(search)** add a search affordance to the Api request panel, matching whatever the app's one shared search component already is; then a **full sweep of every panel in the app** (Studio and Api) that could support search — gRPC is named as a known gap, but the sweep must not stop at the panels named here — giving each a search button using that one shared component, uniformly; **(method colouring)** the Api method select's coloured background replaced with coloured *text* on a neutral background (re-check against P17 N1, which already coloured the method select — the user is reporting the background treatment itself as the problem, not absence of colour), with the identical treatment applied to Studio's connections panel wherever a method/kind chip currently uses a background fill; **(field descriptions)** every configurable row across the request builder — headers, params, form-data fields, and any other row-shaped configurable field this phase's own investigation finds — gets a Description field, the same way P17 added one to variables/environments; **(collections panel)** the Environments list becomes its own collapsible category at the bottom of the collection tree panel (alongside the existing folder/request tree, not inside it), and clicking an environment opens it in a new tab (re-check against P17 N4, which already made environment editing open as a tab — this item is about the tree's own presentation, not the editor surface); **(variables/environments views)** a general layout pass — elements described as "all over the place," inputs not filling available width, uneven spacing; **(gRPC)** the method select is too short relative to the address field — bring it to the same length, and the Call button's label text is a low-contrast/invisible grey, apparently unstyled — fix both; **(mode buttons)** the Studio/Api mode-switch icons are still not aligned with their labels (re-check against P15 §3 and P18 item 4/S11, both of which already claimed this exact fix — a third report means either a real regression or a gap those fixes didn't cover); **(Mongo)** the console/collection "copy" action only emits plain JSON — add Shell Mode (`mongosh`-style, e.g. `ObjectId("...")`/`ISODate("...")` constructor syntax) and Canonical Extended JSON (`{"$oid": "..."}`/`{"$date": {"$numberLong": "..."}}`) as selectable copy formats, alongside the existing plain-JSON one; **(active-state colour)** the active-filter/order-by indicator introduced by P19 item 5 (`--kira-state-on`, a teal/green) should be gold/amber instead — re-pick the token's value (or introduce a differently-named token if a genuine second "on" state is needed) rather than reopening the whole indicator mechanism P19 already built; **(console selection)** the query console's result-selection visuals (from P19 item 3's `SlickHybridSelectionModel` port) don't match the SQL data view's own selection look — bring them to the same visual treatment, since both now share the same selection model; **(status tooltip)** the CPU/memory status-bar tooltip's long explanatory text trimmed down — keep the numbers, drop the essay; **(window state)** on app close, remember which top-level module (Studio or Api) was active per window and reopen into that module, not always defaulting to one; **(SQL format)** the Format button is reported broken again — re-investigate against the real current tree rather than assuming P19's "not actually broken, three separate causes" diagnosis still holds, since behaviour may have shifted since; **(autocomplete popup styling)** every autocomplete dropdown in the app (SQL/Mongo completion, header names, variable names, faker functions, and any other) should render through one visually uniform popup component/style, not each surface's own look; and **(schema-aware SQL/Mongo completion — corrects a real misunderstanding in how P19 shipped this)** table/column completion must be driven by the schema metadata this app *already fetches and caches from the live connection* for the project tree and other Studio surfaces, not by a hand-pasted "Schema (DDL)" document — P19's "Fill from connection" button, which stages that same cached metadata into a document a user must remember to open and click, was the wrong shape for what was asked; the live cache should back completion directly and automatically wherever a connection is already open, with no separate manual step, and without ever having the language layer connect to the real database itself (the DDL/LSP design point was itself the misunderstanding — the fetched-and-cached metadata this app already holds is enough); **(request/response split)** the divider between the request and response panes is not visibly rendered — give it a real, visible border/divider treatment; and **(row insert scroll)** adding a new row to a table (the request builder's headers/params/form-data tables and any other row-insertable table this phase's own investigation finds) does not scroll the newly added row into view — it should | Recorded as one durable batch rather than living only in chat before its own plan exists, per this repo's own convention (P16-P19). Six items explicitly re-check claims from P15/P15b/P16/P17/P18/P19 that this batch reports as still-wrong or regressed (status-code visibility, variable colouring/hover/autocomplete, SQL-grid arrow position, method-select styling, mode-button icon alignment, and SQL Format) — this phase's plan must root-cause each against the real current tree rather than assume either the user's report or the prior phase's own "Implemented" note is automatically right, the same discipline P19 itself applied to its own inherited regressions. The schema-completion item is the one item that is a correction of a misunderstanding rather than a regression or new ask — P19's plan explicitly chose the DDL-document design, and this phase's own plan must revisit that choice against what's actually cached today (`TreeService.Definition` and whatever else already backs the project tree) rather than re-litigate the "no live DB connection from the language layer" constraint, which stands. Sequenced after P21 per this repo's one-phase-at-a-time discipline. **Plan: `plans/P22-shared-primitives-and-tokens.md`, `plans/P22b-api-and-studio-polish.md` and `plans/P22c-schema-aware-completion.md`** — split three ways rather than one document, on the P15/P15b precedent this row itself invites: part 1 is the shared token/primitive/shell layer both modes sit on and must land first (parts 2 and 3 consume its control-height roles, its disabled-primary contrast fix and its shared completion-popup block); part 2 is the per-surface Api and Studio items; part 3 is the one architectural item, and the only one touching Go, the wire and the SQLite schema. Between them the plans correct **nine of this row's own premises against the real tree**, the way P19's row records its four: (1) the control-height token scale the row asks to be created already exists (`--kira-h-xs/sm/md/lg`) and every form primitive already reads it, so what is built instead is a *role* layer over it plus the literals that bypass it — and a chrome-bar token family (`--kira-bar-h` and friends) landed by hand mid-planning, splitting `.p-toolbar` (34px) from `.p-view-head` (28px), which is a better candidate for the reported height inconsistency than any control; (2) "the page-number input is taller than its neighbours" is not reproducible from the stylesheet — every control in that row is `--kira-h-sm` under one `box-sizing: border-box` — so the plan ships a measurement before a fix; (3) P16's claim that the pager sits "~8px from the grid's right edge" is wrong by ~76px, and the cause is P16's own D2 (`RunState` renders after `#toolbar-end` with a permanent `7ch` reservation); (4) Studio has no method/kind chip with a background fill to convert — its tree renders an `EngineIcon`, so the method-colour change lands entirely in Api; (5) the console/grid selection *fill* already matches — what the console lacks is the `sel-t/-r/-b/-l` perimeter layer P19's port deliberately dropped; (6) the response status code's meaning has not been hover-only since P15/P18 (it is an always-visible line in both response panes, and the tooltip was removed on purpose) — the real gap is every *secondary* status surface; (7) the Mongo copy action already emits both requested formats (shell via P27 D12, canonical extended JSON via P19 D6) — what is missing is their naming and a submenu, not the encoders; (8) the SQL Format button's three P19 causes are all still fixed, but P19's own D13 introduced a new defect — `out.join(';\n\n')` silently deletes the document's final semicolon on every press; (9) the tree's cached metadata does **not** contain columns (P19 D5 made a relation a leaf), so the row's "the schema metadata this app already caches" is served instead by the SQLite-backed `metadata_cache`, which does hold `ObjectMeta.Columns` and is readable with no live connection at all — part 3 adds a one-round-trip-per-container `SchemaColumns` adapter capability, caches it as a fourth kind on the container's existing cache row (zero net rows against the 200-row cap), and deletes P19's "Fill from connection" button and no-schema hint strip. Also corrected: the gRPC "Call button is an invisible grey" item is not a gRPC bug at all but a `.p-btn:disabled` cascade defect rendering **every** disabled primary button in the app at a measured 1.13:1 contrast; and "add a search affordance to the Api request panel" plus "gRPC is a known search gap" are both already partly shipped, so the sweep is re-scoped to the five surfaces that genuinely have none plus the one that has a non-shared one. The two items added late (request/response divider, row-insert scroll) are covered in part 1 (`PanelSplitter` is fully transparent at rest in all five consumers; `CellEditorDock` works around it locally and the two request views do not) and part 2 (two distinct add idioms — a trailing blank row in four tables, and the SQL grid's staged-insert button whose own watcher renders but never calls `scrollRowIntoView`) | **P22 part 1 (shared primitives and tokens) implemented** — sixteen commits (T1-T14, with T8 and T10 each split into two per the plan's own commit sequence) plus one follow-up test fix landed on `claude/feature-v1-2`; T15 is folded into each D-item's own commit rather than gathered separately (the plan's own §4.3 test cases are each already present, verified against the real tree rather than assumed), and this row is T16. D1 built the `--kira-control-h/-lg/-sm`/`--kira-control-inline-h`/`--kira-viewhead-h` role layer as a pure alias over the existing `--kira-h-*` scale — `control-sizing.spec.ts`'s D1 no-op case pins every family's rendered height unchanged (22/22/26/18px) at the 12px default, with `.p-view-head` the one deliberate exception (28→34px, closing F2(c)'s split with `.p-toolbar`, both now `--kira-bar-h`). D2's own premise was measured before it was fixed — the pager row already rendered as one height under one `box-sizing`, so `control-sizing.spec.ts` ships the measurement itself. D3 replaced a disabled primary button's full-strength fill with an illegible label with a uniform 0.45 opacity dim, on both `.p-btn.primary` and `.p-iconbtn.is-primary`. D4 moved `RunState` ahead of `#toolbar-end` in `ViewChrome`, closing the ~76px gap F6 found between the pager and the grid's own right edge, in both the data grid and `DocumentView` (OQ-4). D5 replaced the mode-tab guard's box-level assertion with a real ink measurement (screenshotting each tab's icon/label and diffing against its own sampled background) — `mode-switch.spec.ts`'s new case is intentionally red at the phase's own baseline. D6 renders the mode-tab icon at native 16px (was 13px in a 16px box) and shares one line-height between icon and label; this sandbox's own headless-Chromium measurement found a real 4px→3px inset improvement, not the near-total fix the plan's own hypothesis expected — a residual, permanent per-glyph side-bearing difference that D6 documents rather than chases further. D7 dropped `.p-method`'s tinted background everywhere it appears, keeping only its text colour, and deleted the `filter: brightness()` hover workaround the fill had needed. D8 hoisted one `.p-completion*` block into `primitives.css` and mirrored its metrics onto CodeMirror's own tooltip DOM (`editor/theme.ts`), including its per-kind icon glyphs, so the plain-field and console popups now share one visual spec. D9 re-picked `--kira-state-on` to `#e0a33c`, a gold/amber replacing the teal P19 introduced, the value pre-resolved by the plan's own OQ-2 against F16's four constraints. D10 promoted `computeSelEdgeHashes` to `views/shared/slick/selectionEdges.ts` (the `biome.json` cross-import boundary the move exists for) and wired the console's own result grid to the same `sel-t/-r/-b/-l` perimeter layer the SQL data grid already had. D11 trimmed the status-bar CPU/memory tooltip to its numbers, dropping the explanatory sentences, carrying the one load-bearing sentence forward into `docs/ARCHITECTURE.md`'s own CPU-convention paragraph so that page stays accurate. D12 gave each window a persisted `mode` column (migration 0014, `NormalizeMode` dropping an unknown value back to `'studio'`, `repos/windows.go`'s `GetMode`/`SetMode` mirroring `SetBounds`'s own shape) and retired the frontend's old implicit boot-mode heuristic — deriving mode from whichever restored tab happened to be marked active — in favour of `windowsEnsure`'s own answer; per D12's own named fork, this app's real per-window bounds persistence turned out to be entirely Go-side/native-event-driven with no frontend IPC counterpart to piggyback a shutdown-time write onto, so `setMode` instead debounces its own `windowsSetMode` write the same way `state/layout.ts`'s `patchLayout` already debounces a layout write, never synchronously on the click that changes it (F20's own invariant, still proven by the pre-existing "mode switching writes nothing" case). D13 gave `PanelSplitter` an opt-in `divider` prop drawing a real box-shadow hairline at rest, applied to the three consumers that reported the request/response split as invisible (`HttpRequestView`, `GrpcRequestView`, `CellEditorDock`, the last dropping its own local `border-top` workaround in favour of the shared prop) — deliberately not applied to `WorkbenchShell`'s two splitters, whose grid gap already reads as a groove (§5). Explicitly not done, per the plan's own §5/§6: `--kira-control-h`'s value is not raised (OQ-1 — the mechanism is built, picking a new number is a judgement call for a human at the app); the codicon glyph set is not replaced with inline SVG (OQ-3), and D6's `--kira-icon-optical-y` escape-hatch token is left at its default `0px`, wired but unused, so that fix has one clean place to land later; `--kira-bar-h`'s own meaning (introduced by a concurrent commit while this plan was being written) is not redefined; and no rendered control height changes anywhere outside `.p-view-head`. Validation: `bun run lint`/`typecheck`/`build` clean; `go build`/`go vet ./apps/kira-studio/internal/...` clean; `go test ./apps/kira-studio/internal/...` all pass (including new `migrate_window_mode_test.go` and `windows_test.go` cases); `bun run test:unit` 657/657; the full `bun run test:ui` suite passes except the one documented pre-existing flake (`budgets.spec.ts`'s scroll p50 threshold, confirmed passing in isolation). **P22b (per-surface Api/Studio polish) implemented** — twenty-two commits (U1-U22), one follow-up regression-fix commit found by the full `test:ui` run below, plus this row (U23) landed on `claude/feature-v1-2`, following part 1. D1 gave the four bare secondary status surfaces (HTTP's history list/timeline/diff dialog, gRPC's call history) the same `v-tooltip="statusHint(...)"` the two response panes already carried, leaving F1's own "still an inline line, never tooltip-only" guarantee on those two untouched. D2 added `headerValueCompletions` to `packages/api-core` (a whole-value vocabulary for `Content-Type`/`Accept`/etc., a prefix vocabulary — `Bearer `, `Basic `, … — for `Authorization`, composing with rather than replacing the `{{variable}}` source) and wired it into the value cell as a function of the row's own name. D3 moved Save into `#head-trailing` in both `HttpRequestView`/`GrpcRequestView`, the slot `ViewHeader` already reserved for it, so its position no longer shifts with the dirty mark or an unresolved-variable chip. D4 gave a resolved `{{variable}}` its own `--kira-var-resolved` token, freed up by part 1's own D9 vacating that hue, closing the "identical to `--kira-syntax-property`" collision D4's own investigation found. D5 wired `variableHoverSource`/`variableCompletionSource` onto the HTTP raw/code body editors and the gRPC message editor, the one place `VariableSupport` had range-highlighting but no hover or completion. D6/D7 added a `description` field to every configurable row this phase could find a real slot for — HTTP headers/urlencoded/form-data fields and gRPC metadata rows get it directly on the row; query params, which have no persisted row at all (D9's URL-is-authoritative design), get it through a `paramDescriptions` side-car map reconciled on every URL edit — behind a per-tab `fieldDescriptions` toggle, off by default. D8 added an Environments category to the collections panel, collapsed by default, alongside the existing tree — a second entry point onto the same `openVariableSetTab` call `EnvironmentsDialog`'s own row click already makes; the dialog itself is untouched. D9 replaced independent per-field `flex` values with one CSS Grid template shared by `FieldRowsTable`/`MetadataTable`/`VariableRow`/`VariablesOverviewPanel`, so a secret row's extra affordance no longer shifts its neighbours' columns out of alignment, plus the `inheritAttrs: false` wrapper fix for the environment/variable views' own inputs not filling their width. D10 gave the gRPC method `<select>` the same wrapper + `:deep(.p-select)` idiom the target field already used, so it no longer shrinks to its widest `<option>`. D11 turned the Mongo document/console "copy" actions into a named three-format submenu (Shell / Canonical Extended JSON / Relaxed Extended JSON — the last a new `canonicalToRelaxed` in `views/shared/document/ejson.ts`), matching the SQL grid's own `Copy row(s) ▸` idiom, for both a single document and a whole result set. D12 fixed `formatConsoleText`'s own terminator bug — `splitSqlStatements` excludes the trailing `;` from each statement's text, and the formatter's `join(';\n\n')` silently dropped the document's own final one on every press. D13 reworded the "Already formatted" note now that a same-text result is a real no-op rather than evidence of the D12 bug. D14 gave the five surfaces `PanelSearchBox`/`ResponseFindBar` genuinely never reached — gRPC's response pane (find-in-message) and call history (an always-visible filter, HTTP's own `ResponseHistoryList` idiom), `DefinitionView` (find-in-source plus a plain substring filter over Structure's columns/indexes/constraints), the cell editor (find-in-value) — plus one correction to the row's own premise: `OperationsPanel`'s op log already had a working, always-visible filter (an existing `TextField` with `icon="filter"`, missed by a `icon="search"`-only grep) that already matched command/kind/error, so the one real gap — the connection's own name — was closed in its existing predicate instead of bolting on a second search box beside a perfectly good one. D15 converted `BrowseView`'s own hand-rolled always-visible `.filter-field` to the same toggle-plus-`PanelSearchBox` idiom, `rt.filter`/`filteredNodes`/the `N of M` caption and every `data-testid` unchanged. D16 gave `FieldRowsTable`/`MetadataTable`/`VariableSetView` a three-line watcher scrolling their own newly-appeared trailing blank row into view on growth (never on a shrink); D17 gave the SQL grid's own `inserts.length` watcher the one `grid.scrollRowIntoView` call its own comment already justified. Explicitly not done, per the plan's own §0.4/§5: query params still have no persisted row of their own (F10's real structural constraint, not an oversight); `keywordCase: 'preserve'` is not reversed (P13 D4's ClickHouse hazard stands); the gRPC Call button's own contrast is part 1 D3's fix, not touched here; no description reaches the wire in either protocol, in any format. Validation: `bun run lint`/`typecheck`/`build` clean; `go build`/`go vet ./...` clean; `go test ./...` all pass; bindings regenerated (`wails3 task common:generate:bindings`) and confirmed to carry `description`/`paramDescriptions` on every affected model; `bun run test:unit` 673/673; the full `bun run test:ui` suite (235 tests, run synchronously to completion, twice) passes except the one documented pre-existing flake (`budgets.spec.ts`'s scroll p50 threshold — 15ms/29ms against a 12ms budget under full-suite worker contention, confirmed passing in isolation at 11ms both times it was rerun alone). Three real regressions this same full run caught (none visible from any single commit's own lint/typecheck/build) were fixed rather than papered over: `CollectionsPanel.vue`'s new `initVariables()` call (D8) raced `EnvironmentSelect.vue`'s pre-existing one on a fresh Api-mode mount, double-firing `variablesListEnvironments`; `resultMenu.ts`'s "Copy all ▸ Shell mode" (D11) mapped every document through the always-pretty-printing `toShellText`, producing N multi-line blocks instead of one compact line per document; and a pre-existing gRPC toolbar-width test's own assumption ("the target field absorbs virtually the whole resize delta") went stale the moment D10 made the method select `flex: 1` too, so it was updated to assert both fields grow together instead of one absorbing it all. `tests/e2e-real` case 6 (a described header never reaches the wire) was verified by inspection rather than a new real-backend test file: `internal/httpclient.Header` has only `Name`/`Value` fields and the package carries zero references to `Description` anywhere, so the field cannot reach the wire by construction.

**P22 part 3 (P22c, schema-aware completion) implemented** — fourteen commits (V1-V14) landed on `claude/feature-v1-2`, in the plan's own dependency order. V1 added `SchemaColumns` to the `Adapter` interface plus `model.RelationColumns`/`ValidateRelationColumns` and the `Caps.SchemaColumns` flag (true for the five SQL kinds, false for mongo/redis/kafka/sqs/s3 — each of the five now returns `adapters.Unsupported` for it). V2-V4 gave postgres, mysqlfamily (shared by mysql/mariadb) and sqlite/clickhouse their own `listSchemaColumns` — a widened form of each adapter's existing per-object column query (a schema-wide `WHERE`, no `relname`/`TABLE_NAME` filter), never a loop over relations: postgres and mysqlfamily fold `IsPrimaryKey` into the same query (a cheap join/existing `COLUMN_KEY` column, not `listIndexes`' own per-relation cost); sqlite reuses `relevantTables` plus one `pragma_table_xinfo` join across every one of its tables; clickhouse reuses `relevantTables` plus one schema-wide `system.columns` query, `IsPrimaryKey` always false per its own D18/D23 rule. V5 added `tree.Service.SchemaColumns`, a fourth `metadata_cache` kind (`columns`) sharing the container's existing `(connection_id, path)` row rather than adding one per relation (F8's 200-row budget), with the identical cache-aside/`requireConnected`-after-cache-check shape `Children`/`Describe`/`Definition` already have. V6 added `bridge.TreeService.SchemaColumns` (`TreeDescribeArgs` reused, `tabId` unused) and regenerated bindings. V7 added `state/schemaColumns.ts` (`ensureSchemaColumns`, idempotent/single-flight/fire-and-forget) and wired it into `ConsoleView.vue` (mount + container-path watch) and is invalidated off the same `onConnectionMetadataInvalidated` broadcast `project/state/tree.ts` already subscribes to. V8 widened `sqlCompletionSources`/`consoleCompletionSources` with a `cached: RelationColumns[]` parameter and a new `namespaceFromCached` (`ddl.ts`) projecting it into lang-sql's own namespace shape byte-for-byte like `toSqlNamespace` — precedence is document (wins wholesale) → cache → tree relations → `undefined`, never a per-table merge. V9 added `effectiveSchema` (`state/schemaColumns.ts`), the DdlSchema-shaped projection lint/hover read through `ConsoleView.vue`'s own `ddlSchema` computed, so completion/diagnostics/hover can never disagree about what the console knows. V10 (`refactor(studio)!`) deleted P19 D15's "Fill from connection" button/progress state and D16's no-schema hint strip and dismissal state — the Schema (DDL) dialog, `saveDdl`/`ensureDdl`/`parseDdl` and the connection-row menu entry are unchanged, now framed as a deliberate override rather than the only supply. V11 promoted `mongoFilterCandidates`/`mongoSortCandidates` to `views/shared/mongoFieldSample.ts` (mandatory: `views/console/**` may not import `views/documents/**`) and added a fourth Mongo-console completion position — a bare identifier inside a `db.<collection>.<method>(…)` call offers that collection's own sampled field names (pushed by `DocumentView.vue` on page load, read by the console, never fetched), merged with the existing BSON-constructor list. V12 covers the plan's own §4 verification (below). V13 recorded the fourth `metadata_cache` kind in `docs/ARCHITECTURE.md`'s Caching section, including the two open items it hands to P24 (§5). Validation: `bun run lint`/`typecheck`/`build` clean; `go build`/`go vet ./apps/kira-studio/internal/...` clean; `go test ./...` all green, including new per-adapter `SchemaColumns` conformance cases (postgres/mariadb/mysql/sqlite/clickhouse, each asserting DataType/IsPrimaryKey byte-identical to `Describe`'s own answer for the same column, plus an empty-container case) run against real containers, the five non-SQL kinds' `Caps().SchemaColumns=false`/`E_UNSUPPORTED` cases, and `internal/tree`'s new cache-aside suite (miss-then-hit, cache-hit-while-disconnected, refresh-bypass, invalid-payload-drop, `Invalidate`, and the F8 shared-row proof); bindings regenerated and confirmed (`-tags server` build plus `tests/e2e-real/{sqlite,postgres,mariadb}-real.spec.ts` all pass against real backends); `bun run test:unit` 666/666 (new `namespaceFromCached`/`effectiveSchema` suites); the full `bun run test:ui` suite passes except the one documented pre-existing flake (`budgets.spec.ts`'s scroll p50 threshold, reproduced in isolation both before and independent of this phase's changes). Deliberately not done, per the plan's own §6/§7: no staleness/TTL policy for the new cache kind (P24's question); no cross-schema completion (OQ-3); no indexes/foreign keys in `SchemaColumns`' own payload (OQ-2) |
| **P23 (new) SQLite storage unbounded-growth audit** | A dedicated audit of every table and cache in the app's own `kira.db` (and any on-disk cache alongside it) for unbounded accumulation, beyond the response/gRPC-history caps already enforced (P8/P18/P21). This phase's own plan must enumerate every write path into local storage — connections, tabs, collections, variables/environment history, oplog, metrics/telemetry if any, and any other table `internal/storage/repos` defines — and for each one, either confirm a real, enforced cap/eviction/pruning policy already exists, or design and add one where none does. Fixing what's found is this phase's job, not just reporting it | The user's own concern is durable and general — "make sure nothing accumulates without limit" — not scoped to the tables this chapter's own review rounds happened to already touch (variable history, response/gRPC history). A dedicated pass across the whole schema is the only way to actually answer that, rather than relying on whatever individual phases happened to cap along the way. Sequenced after P22 per this repo's one-phase-at-a-time discipline. **Plan: `plans/P23-sqlite-unbounded-growth-audit.md`.** The audit read all **nineteen** tables the schema defines (`internal/storage/migrations/*.sql` for the schema, `internal/storage/repos/*.go` for the write paths) and found the app **already better bounded than this row assumes**: seventeen are bounded today. `api_response_history` (256 KiB/body, 30/scope, 128 MiB table budget, launch orphan sweep) and `grpc_call_history` (64 KiB/message, 100 messages/entry, 30/scope, 32 MiB budget, orphan sweep) are the model the rest is measured against; `metadata_cache` is capped three ways (4 MiB/row, 200 rows/connection, and dropped whole on every reconnect); `api_variable_history` trims to 20 per variable. Four of this row's own premises are wrong and the plan corrects them: **none** of the five tables the row names (connections, tabs, collections, variables/environment history) is a gap — `tabs` is rewritten per window on every save and cascades on window close, and the rest grow only when a person creates something; there is **no metrics/telemetry storage at all** (`internal/metrics` is an in-memory ticker with no table); and there is **no on-disk cache alongside `kira.db`** — `~/.kira-studio/` holds only the database, its `-wal`/`-shm`, and `logs/`, which `internal/logging/sweep.go` already ages out at 30 days. What is genuinely unbounded is **two tables the row barely mentions**, both in the same way (a count cap exists, a byte cap does not): **`op_log`**, whose `command` column stores an entire pasted console document verbatim (`postgres/console.go`'s `SetCommand(joinSemicolons(statements))`) with nothing truncating it on any path, so a 5 MB script re-run twenty times is 100 MB the 20,000-row cap never touches; and **`filter_history`**, capped at 20 rows per `(connection_id, path)` with no bound at all on the number of paths, no byte cap, and no liveness oracle. The plan adds `op_log` a 64 KiB `command` cap (derived twice over: `grpc_call_history`'s own per-message figure, and the largest value keeping the table under twice its budget between two 500-op prunes), an 8 KiB `error` cap, a `stored_bytes` column plus covering index, and a 32 MiB table budget swept as a third pass in the existing `Prune` — plus a `command_truncated` flag, because the Operations panel's **Re-run** executes `command` and must not run half a script. `filter_history` gets a 4,000-row per-connection cap (`metadata_cache`'s own 200-paths-per-connection × this table's own 20-per-path) and a 4 KiB per-row text cap. One finding the row did not ask for is in scope because it decides whether any cap actually helps: **nothing in the app ever returns freed pages to the filesystem** — no `VACUUM`, no `auto_vacuum` anywhere — so `kira.db` is a permanent high-water mark; the plan sets `auto_vacuum=INCREMENTAL` on new databases plus a thresholded startup `incremental_vacuum`, and a 4 MiB `journal_size_limit` so `kira.db-wal` stops keeping one transaction's high-water mark for the session. All of it measured in-worktree against the pinned `modernc.org/sqlite v1.57.0`, not assumed from SQLite's documentation. **Implemented** — seven commits (T1-T7) plus this row (T8) landed on `claude/feature-v1-2`. T1 added migration `0015_p23_op_log_bytes.sql` (`stored_bytes`/`command_truncated` columns, a byte-counted backfill via `CAST(...AS BLOB)`, a purge of any pre-existing row already over the combined cap, and the `op_log_bytes` covering index) and taught `OpsRepo.Finish` to truncate `command`/`error` to 64 KiB/8 KiB in place, threading `commandTruncated` onto `model.OpRecord`/`opRecordSchema` and through `oplog.Wiring` so a live push and a later reload never disagree about what was actually stored. T2 gave `Prune` its third pass — the 32 MiB table-wide byte sweep, gated behind the new covering index. T3 disabled **Re-run** (not **Copy command**) on a truncated row in `OperationsPanel.vue`. T4 gave `filter_history` the 4,000-row per-connection cap and the 4 KiB per-row text cap. T5 set `_auto_vacuum=INCREMENTAL` and `journal_size_limit(4194304)` in `buildDSN`, plus `repos.Maintenance.Reclaim` (a startup `incremental_vacuum` above 16 MiB of freelist), wired beside the two existing `SweepOrphans` calls in `main.go`. T6 added the owed test coverage: `ops_test.go`/`ops_internal_test.go` (new — op_log's four interacting rules: truncation-with-flag, truncation-without-flag, the byte sweep's oldest-first/never-empties invariants, and regression coverage for the pre-existing retention cut and 20,000-row hard cap, neither tested before this phase), `history_bytes_index_test.go` (extended — `op_log`'s own `SUM(stored_bytes)` query plan uses the covering index), `filter_history_test.go` (extended — the per-path cap regression, the per-connection cap evicting the least-recently-used path while the most-recently-used survives, and the per-row text truncation), `migrate_op_log_bytes_test.go` (new — migration 15 against seeded data, including a non-ASCII command proving the backfill counts bytes, not SQLite's own character-counting `length()`), `dsn_test.go` (new — a freshly-opened database reads back `PRAGMA auto_vacuum = 2` and `PRAGMA journal_size_limit = 4194304`), and one `operations.spec.ts` case (commandTruncated disables Re-run, not Copy command). T7 recorded the audit's answer in `docs/ARCHITECTURE.md` — the growth-bound table above, the standard a new table has to meet, and the doc/schema drift F1 found (`connection_filters` → the real `connection_tree_filters`). Validation: `bun run lint`/`typecheck`/`build` clean; `go build ./...`/`go vet ./...` clean; bindings regenerated (`wails3 task common:generate:bindings`) and confirmed to carry `commandTruncated` on `OpRecord`; `go test ./...` all green (every package, real containers included); a `-tags server` build plus `tests/e2e-real/sqlite-real.spec.ts` passed against a real backend — the check this phase's own bindings-regeneration lesson calls for, since T1 changes a bridge-visible type's shape; `bun run test:unit` 682/682; the full `bun run test:ui` suite (238 tests, run synchronously to completion) passes except the one documented pre-existing flake (`budgets.spec.ts`'s scroll p50 threshold — 13-14ms against a 12ms budget under full-suite worker contention, confirmed passing in isolation both times it was rerun alone, at p50=11ms). |
| **P24 (new) DB metadata cache staleness audit** | Confirm and, where missing, fix the intended behaviour: connecting to a database (after being idle a while) should refresh its cached metadata (schemas/tables/columns/indexes, whatever the tree and completion layers already cache), but simply **navigating** the already-loaded tree (opening tables, expanding nodes) should serve from that cache and must not re-fetch from the live database each time — only an explicit user-initiated refresh, or the cache actually going stale by the app's own definition of stale, should trigger a re-fetch. This phase's own plan must locate the actual cause of the over-fetching the user is observing (a missing staleness check, a cache keyed too narrowly, an invalidation that fires too eagerly, or similar) against the real code in `internal/adapters`/whatever owns the tree/schema cache, rather than assume a specific mechanism up front | Directly enables P22's schema-aware SQL/Mongo completion item to be cheap in practice, and is also a correctness/performance concern on its own regardless of that dependency — recorded as its own phase since the user described it as a distinct, general problem, not scoped to autocomplete alone. Sequenced after P23 per this repo's one-phase-at-a-time discipline. Plan: `plans/P24-metadata-cache-staleness.md` — whose investigation corrects this row's own framing, and the correction changes what gets built. Of the three behaviours the user described, **two already hold and the third does not — and the one that "holds" is the cause of the problem**: (1) a successful connect *does* refresh, by `DELETE`-ing every `metadata_cache` row for that connection (`connections/service.go:639`) — which is not a refresh but a wipe, so every node, table and console opened afterwards is a cold server fetch with nothing left to hit, which is exactly the "fetched and refreshed all the time" being reported, arrived at from the opposite direction to the one this row assumed; (2) navigating a live tree genuinely does **not** re-fetch — `expand()` early-returns on the renderer's own copy (`project/state/tree.ts:164`), the grid's `describe`, the definition view's `definition` and P22c's `ensureSchemaColumns` are each once-per-tab and read through the cache, nothing polls and nothing auto-connects, so this half of the user's diagnosis is not a bug and the plan does not manufacture one; (3) an explicit *Refresh* is the one that is broken — it re-fetches exactly **one kind at one path**, leaving the other three kinds on the same merged `(connection_id, path)` row untouched; a connection's *Refresh* re-lists only that connection's root rather than "all rows for that connection" as `docs/v1/plans/P1-connections-and-tree.md` §6c's own eviction table specifies; `TreeService.Invalidate` (the Go API built for exactly that) has **zero frontend callers**; and Refresh on a *table* calls `Children` on a leaf — a real engine op and an op-log row producing an empty list nobody renders, while the `describe`/`definition`/`columns` payloads the user actually wants re-read stay stale. Underneath all of it: **there is no staleness concept anywhere** — `fetched_at` is written on every write and read by exactly one statement, the eviction `ORDER BY`; `etag` has never been written non-`NULL`; a cache hit wins at any age. The fix is therefore not a TTL (which would re-fetch mid-session, violating behaviour 2, *and* miss a reconnect inside its own window, violating behaviour 1) but a definition: **stale means "written before the connection was most recently established"**, using `ConnectionState.Since` as the epoch and a per-kind timestamp stored under a reserved `fetchedAt` key inside `payload_json` — no migration, no new column, no new bridge method, and `fetched_at` finally gets a reader. A connect then stops deleting and merely moves the epoch (rows are re-read lazily, once each, only for the paths a user actually opens, and survive for the disconnected/launch read P1 D10 designed and P22c F7 depends on), and an explicit Refresh routes through the dead `Invalidate` and drops the whole row. **Implemented** — G1-G4 (Go) gave `metadata_cache.Get` a per-kind `fetchedAt` map and taught `tree.Service.getCached` to bypass (never drop) a payload written before the connection's own `Since`, then removed `attemptConnect`'s `DropConnection` call now that the freshness rule makes it redundant; G5 is the resulting `internal/tree` staleness suite, including the per-kind hazard case that fails against a row-level `fetched_at` and passes only against the per-kind map, plus one `internal/connections` case pinning that a successful connect keeps the rows and still emits `metadataInvalidated`. F1-F3 (frontend) route every tree Refresh through `treeInvalidate` instead of `loadChildren`'s own `refresh:true` — `refresh`/`refreshConnection`/`refreshObject` each drop the whole row (or every row for a connection) before reading again, `menus.ts` sends a leaf's Refresh to `refreshObject` so it no longer calls `Children` on a table/collection, and `dropSchemaColumns` clears the console's own completion cache on every explicit refresh, not only a reconnect. V1 extends `tests/ui/tree.spec.ts` with both the container- and leaf-refresh assertions (the leaf one confirmed to fail against a deliberately reintroduced regression, both checked via `control.log()` rather than `opsCount()` where that is the more precise instrument); V2 is this row plus `docs/ARCHITECTURE.md`'s Caching section and P23's own inventory line. Validation: `bun run lint`/`typecheck`/`build` clean; `go build`/`go vet ./apps/kira-studio/internal/...` clean; `go test ./...` 585/585 green (18 new `internal/tree` cases, 2 new `internal/storage/repos` cases, 1 new `internal/connections` case); `bun run test:unit` 682/682; `bun run test:ui` 238/238 (one `budgets.spec.ts` failure in the full run reproduced as the documented pre-existing cross-file-worker-contention flake, confirmed passing when re-run alone); `tests/e2e-real/{sqlite,postgres}-real.spec.ts` both green against real backends as an extra sanity check on the changed connect path |
| **P25 (new) DataGrip connection import** | Import database connections (including their saved passwords) from a local JetBrains DataGrip project: a file picker lets the user choose the DataGrip project folder; this phase parses DataGrip's own project connection configuration from that folder, and decrypts the saved passwords using the same mechanism DataGrip itself uses — the IntelliJ Platform `PasswordSafe` credential store (an on-disk "IntelliJ platform db," keyed by a database UUID, encrypted with a locally-held master key) — so the import needs no interactive DataGrip/JetBrains login of its own, only read access to files already on disk. This phase's own plan must investigate and document the real on-disk format (DataGrip's connection XML/config layout under the project's `.idea`/workspace files, and the actual `PasswordSafe` storage format/location/encryption scheme for the current JetBrains Platform version) before implementing, since both are third-party formats this repo has no prior code touching. Plan: `plans/P25-datagrip-connection-import.md` — whose investigation confirms most of this row's premises from primary sources and corrects one that decides the design. **Confirmed, with sources:** a project's data sources live in `.idea/dataSources.xml` (`<component name="DataSourceManagerImpl">`, one `<data-source name uuid>` per source with `<driver-ref>`, `<jdbc-driver>`, `<jdbc-url>`) plus `.idea/dataSources.local.xml` (`<component name="dataSourceStorageLocal">`, correlated **by uuid**, carrying `<user-name>`, `<secret-storage>` and a `<database-info dbms="…">` hint), and JetBrains' own documentation states neither file holds the password (the one exception being a URL-only source whose `<jdbc-url>` contains `user:password@`); the credential key the user described is real and exact — `intellij-community`'s `CredentialAttributes.kt` defines `generateServiceName(subsystem, key) = "IntelliJ Platform <subsystem> — <key>"` (em dash), the DB subsystem's key is the data source uuid, and four independent third-party implementations plus a long-standing `security find-generic-password` recipe agree on the resulting `IntelliJ Platform DB — <uuid>`; `c.pwd` (legacy `pdb.pwd`) is a three-line `encryption:` / `isAutoGenerated:` / `value: !!binary` file whose `BUILT_IN` payload is AES/CBC/PKCS5 under the hardcoded ASCII key `Proxy Config Sec` with a 4-byte big-endian IV-length prefix, and `c.kdbx` is a **stock KDBX 3.1** file whose composite key is the standard `SHA256(SHA256(mainKey))`, holding one group `IntelliJ Platform` whose entry titles are those service names — so no interactive prompt is ever needed, since even a user-set master password is written back into `c.pwd` under that same built-in encryption. **The correction that changes the design:** the "on-disk IntelliJ platform db" is only *one* of `PasswordSafe`'s three backends (`KEEPASS`) and is **not the default anywhere** — the default is `KEYCHAIN`, meaning the real macOS Keychain on macOS and the freedesktop Secret Service over `libsecret` on Linux — so the import reads the **system Keychain first** and treats `c.kdbx`/`c.pwd` as the fallback, with `security.xml`'s `PROVIDER` believed when present. **Partially confirmed, and named as such:** the `DB` subsystem string itself appears in no JetBrains source (the database tooling is closed) and rests on four agreeing reimplementations; `<secret-storage>`'s `master_key` / `memory` / `forget` values map to DataGrip's *Forever* / *Until restart* / *Never* by inference from real files, and are used only to word a miss; and whether IntelliJ ever writes a ChaCha20 inner stream inside a KDBX 3.1 file is unknown. **Library adoption:** `gokeepasslib` (MIT, pure Go) does the KDBX half, and the ~150 lines of IntelliJ-specific glue are hand-rolled against a named requirement — no Go library exists for it at all, the only complete implementation is AGPL-3.0 Swift and cannot be vendored into this MIT repo, and DBeaver's own DataGrip-import request is closed unimplemented. **Scope boundary the plan lands on:** an explicitly-versioned first cut — the macOS Keychain, plus the `c.pwd`/`c.kdbx` file store on both macOS and Linux, with Linux's default Secret Service backend (which needs `libsecret` over cgo, deliberately absent from this repo's Go) and Windows DPAPI refused *by name*, and every uncertainty above failing loudly with a specific "unsupported DataGrip/IDE version" message rather than a silently wrong password. No real DataGrip profile could be captured in this environment (commercial, account-gated, no display), so the fixtures are format-exact synthetic ones plus an opt-in real-profile test for a developer machine — the plan says so plainly rather than implying the crypto is already verified end to end against a real file. **Implemented**, per the plan's 14-commit sequence: `internal/datagrip` (project XML parse/correlate, the three-signal engine cascade, `c.pwd`/`c.kdbx` decrypt via `github.com/tobischo/gokeepasslib/v3`, the macOS Keychain query, `Scan`/`Apply`) with 25 Go test cases against generated fixtures (`internal/datagrip/testdata/gen`, `KIRA_DATAGRIP_FIXTURES=write`) covering every named refusal, not just the happy path; `bridge.Dialogs.OpenDirectory`/`FilesService.ChooseFolder` for the project-folder picker; `bridge.DataGripService.Scan`/`.Import`, sending only the project path across the bridge either way; and the Studio-side import dialog (`DataGripImportDialog.vue`, reachable from `ProjectPanel.vue`'s header and `StudioStart.vue`'s first-run screen) with its own `tests/ui/datagrip-import.spec.ts`. The KDBX fixture was independently confirmed against `keepassxc-cli` (a library with zero code in common with `gokeepasslib` or this package), which recovered the fixture password using the raw main-key bytes as the KeePass password — real corroboration, not just a round-trip through the same library that wrote it. Not run here: §4.4.3's opt-in real-DataGrip-profile test (no display, no JetBrains account in this environment). **Corrected post-implementation (keychain-only follow-up):** the user's original request was macOS Keychain only — the `c.pwd`/`c.kdbx` KeePass file-store fallback above was a misreading of the user's own description of the credential store's internals, not something they asked for. That entire fallback (`kdbx.go`, `mainkey.go`, the `gokeepasslib` dependency, the `testdata/passwordsafe/` fixtures and their generator) is removed; `internal/datagrip` now reads the macOS Keychain only, and a `PROVIDER=KEEPASS`-configured project gets a named `credential-store-unsupported` refusal instead of being retried against a file store. See the plan doc's own correction note for the full accounting | The user asked for this directly as a distinct, self-contained capability, not a modification of any existing import path (Postman import is collections, not connections). Sequenced last in this batch since it's the most self-contained of the four new phases — no dependency on P22-P24's work — but still one-phase-at-a-time per this repo's discipline, and correctly scoped as its own phase rather than folded into the P22 polish batch given the real reverse-engineering risk it carries |
| **P26 (new) Separate interface font from data font** | Split the app's single `--kira-font-family` token (`theme/tokens.css`, currently `Menlo, monospace`, applied to `body` in `theme/base.css` and so to every UI chrome surface — panels, dialogs, buttons, labels, tabs — as well as to data/editor surfaces) into two independent tokens: a **data font**, staying exactly as it is today (monospace, one of P28's fifteen `FONT_CHOICES`, user-customizable via the Settings dialog's existing font dropdown) for surfaces that render actual database/API content — the SQL/Mongo/console grids, document/key-value views, CodeMirror-hosted editors, and similar; and a new, separate **interface font** for UI chrome — panels, dialogs, buttons, menus, labels, tab strips, and every other non-data surface — that is **not customizable** (no Settings entry, no dropdown) and is fixed to the same UI font stack VS Code itself uses for its own interface (VS Code's `--vscode-font-family` default: a system-UI sans-serif stack — `-apple-system, BlinkMacSystemFont, "Segoe WPC", "Segoe UI", "Ubuntu", "Droid Sans", sans-serif` — not VS Code's separate, user-configurable *editor* font, which stays monospace and is the wrong precedent to copy here). This phase's own plan must inventory every current consumer of `--kira-font-family`/`--kira-font-size` (`grep` above found ~30 files) and classify each as UI-chrome (moves to the new interface-font token) or data-surface (keeps the existing customizable token), rather than assume the split along component-folder lines is exact. Plan: `plans/P26-interface-font-split.md` — whose investigation corrects five of this row's premises and leaves the phase smaller than the row assumes. **The split is not new design work; it is a reversion.** The design system this app's `primitives.css` was ported from has always carried two type roles (`docs/design/kira-design-system/parts/_style.css:29-30`, `--font-ui` / `--font-mono`, under its own heading *"type — TWO roles, FOUR steps"*) and a stated rule for choosing between them — **LAW 08**, *"Mono is for data. If a string came out of a database — a value, an identifier, a query, a duration — it is mono. If the app wrote it, it is UI."* The port collapsed both into one `--kira-font-family`; v1 SPEC §8.2 then wrote the collapse down as intentional (*"one font family + size for the whole app (UI, grid and editors alike)"*) and `docs/v1/plans/P31-…md`'s F14 flagged the resulting divergence and deliberately left it standing. P26 supersedes that §8.2 sentence, and classifies every call site by LAW 08 rather than by a rule invented for the occasion. **The interface half is already half-built**: `primitives.css:158-160`'s `.p-input.ui` hard-codes `-apple-system, "SF Pro Text", system-ui, "Segoe UI", sans-serif` — the repo's one font stack not behind a token — and has four live consumers (`CommandPalette`, `PanelSearchBox`, and the connection dialog's *name* field twice), so a sans face is already rendering in this app today; the plan folds that literal into the new token rather than adding a second definition beside it. **The inventory is 43 `--kira-font-family` hits: 38 real consumers across 25 files**, plus `body`, plus one site (`.p-completion`) that needs a declaration it does not have. Classified: **36 stay data** (the grids and their headers, every CodeMirror surface and its three tooltips, `.mono`'s 60 call sites, `.p-input`/`.p-textarea`, `.p-td`/`.p-th .key`, `.p-run-state`'s duration, verbatim adapter errors, document/stream/console bodies, the HTTP method token, `columns.ts`'s measuring canvas) and **3 move to chrome** (the cell editor's format select and its generate menu, the grid's multi-sort order badge) — plus `body` itself, which is the one edit that carries every chrome surface having no declaration of its own, since Tailwind's preflight gives form controls `font: inherit`. **The exact stack was verified against `microsoft/vscode@17c5935`, and the row's quoted string is wrong**: it omits `system-ui`, the entry VS Code's own comment calls load-bearing on Linux. The plan ships VS Code's platform-agnostic spelling verbatim — `-apple-system, BlinkMacSystemFont, "Segoe WPC", "Segoe UI", system-ui, "Ubuntu", "Droid Sans", sans-serif` — the union of `workbench/browser/media/style.css`'s three per-platform rules (`.mac`/`.windows`/`.linux`) that `src/vs/base/browser/fonts.ts:18`'s `DEFAULT_FONT_FAMILY` exports and `webview/browser/themeing.ts:82` publishes as `--vscode-font-family`, four lines above the separate `--vscode-editor-font-family` this row correctly declines to copy. **`--kira-font-size` is deliberately left alone**: the investigation found the type scale is *not* cleanly role-split (`--kira-t-md` **is** `--kira-font-size`, and its 18 consumers are genuinely mixed — the grid cell and the tree row both), P22 D1 coupled `--kira-t-xs/-sm/-lg` to that setting on purpose one phase ago as its own F2(d) fix, and the Appearance section already accepts one control spanning both roles ("Row height" governs *"the tree, the grid and every list"*); VS Code's own fixed `13px` UI size is recorded as the real counter-argument, with the one-token follow-up named. **The Settings dialog needs no copy change** — it already reads "Data font" / "Data font size" with the helper text *"Grid cells, editors, anything that came out of a database."*, copied verbatim from the design-system artboard drawn for the post-split world; and **`fonts.ts`'s availability probe needs none either**, since both its functions are called only on `appearance.fontFamily` and the fixed stack terminates in `sans-serif`, which always resolves. Two just-landed P22 guarantees would break silently and are handled explicitly: `.p-completion` inherits its family from `body` today and so would diverge from the CodeMirror completion popup P22 D8 unified (it gains an explicit data-font declaration plus the `fontFamily` assertion `autocomplete.spec.ts` never made), and `mode-switch.spec.ts`'s P22 D5/D6 ink measurement was taken against the monospace label and is the phase's expected canary — answered with `--kira-icon-optical-y`, the token P22 D6 built for exactly this, never by relaxing the bound. No Settings entry for the interface font is built, per the user's own sentence, even though the design-system artboard does draw one. **Implemented** — all six commits (T1-T6) landed on `claude/feature-v1-2`: T1 declared `--kira-font-data`/`--kira-font-ui` with zero rendering change; T2 renamed the 36 data sites; T3 moved `body`, the three remaining interface sites and `.p-input.ui`'s literal to `--kira-font-ui`, and gave `.p-completion` its own explicit `--kira-font-data` declaration; T4/T5 are the two guard tests (the completion-popup `fontFamily` parity assertion, and a new `font-roles.spec.ts` asserting data/chrome resolve different tokens); T6 is this row plus `fonts.ts`'s corrected comment. `mode-switch.spec.ts`'s P22 D5/D6 ink test — the phase's own predicted canary — passed unchanged after T3, so `--kira-icon-optical-y` stays undeclared; the escape hatch was not needed | The user asked for this as a distinct, general design fix — the app currently has one font serving two visually and functionally different jobs (dense monospace data display vs. UI chrome), which is also why the existing P28 font-customization dropdown looks like it's changing button/label text when a user only meant to change how table data renders. Recorded as its own phase since it's a design-system-level split touching many files across both Studio and Api, not a small polish item bundled into P22. Sequenced after P25 per this repo's one-phase-at-a-time discipline |
| **P27 (new) Test-suite speed** | Make this chapter's own test tiers substantially faster to run — `go test ./apps/kira-studio/internal/...` (the real-container adapter conformance suites: Postgres/MySQL/MariaDB/ClickHouse/Kafka/Redis/SQS, dominated in a cold run by image pull and container-startup time), `bun run test:unit`, and `bun run test:ui` (the ~240-case mocked-runtime Playwright suite, run with 2 workers). This phase's own plan must first measure where the real time actually goes in each tier — container cold-start vs. warm-reuse cost, Playwright worker count vs. this sandbox's/CI's actual core count, any serialized-when-it-could-be-parallel step, redundant rebuilds, and per-test setup/teardown overhead — rather than assume a specific lever up front, and then apply whichever levers the measurement actually justifies (more Playwright workers/sharding, container reuse across test runs within a session, trimming unnecessary `beforeEach` work, `-p` parallelism tuning for `go test`, or similar). The bar is "a lot faster," not a specific percentage, but the phase's own plan should state what improvement it actually achieved, measured before and after, for each tier Plan: `plans/P27-test-suite-speed.md` — which measured every tier first, in a worktree at `0be9f3e` on a 4-core sandbox with every container image already pulled (so image pull is ~0 and this is the warm dev loop, not a cold first run). **Before, wall clock:** `go test ./apps/kira-studio/internal/...` with a cleaned test cache **95.8 s / 95.5 s**; `bun run test:ui` **351 s (5 m 51 s)**, 245 cases; `bun run test:ipc:fe` **25.9 s**; `bun run typecheck` **11.0 s**; `bun run test:unit` **2.68 s**; `bun run lint` **0.75 s** — **≈ 487 s (8 m 07 s)** end to end, of which `test:ui` is 72 % and `test:go` 20 %. Three of this row's own premises did not survive that measurement: `AGENTS.md` prescribes no `go test -p 1` anywhere (there is no `-p` pin in the repo at all, so nothing is serialized there), image pull is not the dominant cost once images are cached, and `playwright.config.ts` says `workers: '50%'` rather than a literal 2. **What is actually slow, and what the plan proposes for each.** (1) `internal/ipcfixture` alone is 54.9 s of the Go tier's 95.8 s and finishes last: it starts six different containers *one after another* inside one package (39.8 s of test time) and then tears six down serially in `TestMain` (~17 s). `t.Parallel()` cannot fix it — it panics against `ipcfixture/harness.go:49`'s `t.Setenv("KIRA_HOME", …)` — so the plan adds a `Prewarm`/`StopConcurrently` seam over `testsupport`'s existing mutex-guarded fixture memo and calls it from the three `TestMain`s that own more than one container, with no production-code change: **measured 58.9 s → 27.6 s for that package, 95.8 s → 64.4 s for the whole tier**. (2) Starting two Kafka brokers at once exposed a real readiness gap — the testcontainers module's log-based wait fires before the advertised listener serves, so the seed's first request is closed mid-`ApiVersions` (reproduced 4 times); a `client.Ping` poll before `seedKafka` fixes it, and with that plus a memoized `docker info` probe (144 call sites × 60 ms) the tier lands at **57.0 / 58.1 / 58.9 s across three clean runs, −39 %**. (3) `test:ui` is already 95.9 % parallel-efficient at 2 workers, so the only lever is worker count — but raising it alone breaks `budgets.spec.ts`'s 12 ms scroll budget at 4 workers (15 ms) *and* at 3 (22 ms), since that spec measures wall-clock latency on a box it is now sharing with three other WebKits. Only two files carry wall-clock budgets (`budgets` + `perf`, 5.6 % of the tier's test time), so the plan moves those two into their own serial `ui-timing` Playwright project with `dependencies: ['ui']` and takes the rest to `workers: '100%'`: **measured 351 s → 258 s (4 m 18 s), 245 passed, zero failures** — and the two timing specs end up on *better* footing (p50 8 ms uncontended, matching the 2-worker baseline, versus 15/22 ms when simply run at more workers). (4) `ipc-frontend` takes the same `'100%'` change: **25.9 s → 10.1 s**. (5) `typecheck`'s four independent projects run concurrently: **11.0 s → 9.7 s**. It also fixes a second pre-existing flake the extra load surfaces (`grpcclient`'s `startEchoServer` hands out its address before `s.Serve` is scheduled — 3 failures in ~14 runs). **Levers measured and declined, with their numbers:** `-p 8` (52.4 s, ~8 % over the default — but the default is already `GOMAXPROCS`, and a literal would cut parallelism on a bigger dev machine); caching or skipping `bun run build:test` (2.5 s cold, 2.3 s warm — 0.7 % of `test:ui`); making `scripts/setup.sh` cache its bindings step, which it already does — verified rather than assumed (4.43 s first run, **0.135 s** second, bindings step skipped); replacing the suite's 59 `waitForTimeout` sites (≈ 15-20 s of 673 s, and most are deliberate frame pacing that polling would change the meaning of); and container reuse across runs (~25-30 s, declined because the adapter suites mutate and run DDL, so a reused container would carry the previous run's writes into the next). Projected total **≈ 487 s → ≈ 339 s (−30 %)**, to be re-measured after implementation per the plan's §4.4 rather than assumed. No test is deleted, skipped or loosened, and no runner is swapped. **Implemented** — nine commits (T1-T9) landed on `claude/feature-v1-2`, all measured directly in the implementation worktree (also a 4-core sandbox, but consistently slower/noisier than the plan's own prototype box — this worktree's own re-measured *before* baseline for `go test` was **102.5 s**, against the plan's 95.8 s on the identical command). T1 added the `Prewarm`/`StopConcurrently` seam (no caller, inert). T2 added `waitKafkaBrokerReady` (a `client.Ping` poll) ahead of `seedKafka`. T3 wired `Prewarm`/`StopConcurrently` into `ipcfixture`, `adapters/kafka` and `adapters/mysqlfamily`'s `TestMain`s — `ipcfixture` alone measured 58.9 s → 24.9 s standalone, matching the plan's own finding. T4 memoized `IsDockerAvailable` with `sync.Once`. T5 (two commits) fixed the `grpcclient` echo-server race, first with a `connectivity.Ready` gate per the plan's own text, then strengthened to a real `grpc_reflection_v1.ServerReflectionInfo` round trip after direct measurement showed the weaker gate still left a residual failure in this environment — verified clean across `go test -count=50` in isolation. **`go test ./apps/kira-studio/internal/...` real numbers, five clean-cache runs after every Go-side commit: 58.8 s / 58.3 s / 58.3 s / 57.7 s / 58.7 s** — a **≈43 % reduction** from this worktree's own 102.5 s baseline, slightly better than the plan's own −39 %. The Kafka fix held with **zero** failures across every run this phase (dozens, well beyond the plan's own three-run bar). The `grpcclient` fix did not reach the plan's own "zero across three consecutive runs": two of those five runs (and roughly 30-40 % of runs sampled more broadly, including 2 of 5 with the `ui`/Docker dependency chain removed entirely via `--no-deps`) still hit the same `EOF` failure, and — a materially different diagnosis from the plan's own "listener not yet accepting" — reproduced identically under a synthetic all-cores CPU stress with **zero Docker involvement at all**, meaning this sandbox exhibits real host-level scheduling noise a server-readiness gate cannot fully close from test code; closing it completely would need a client-side retry inside `internal/grpcclient`'s own production dial path, out of this phase's own declared scope (no production-code changes). One further run also hit a third, previously-undocumented flake, `internal/shell`'s `TestSecondShouldQuitReturnsTrue` (a 1 s internal handshake timeout exceeded under load) — confirmed clean across 20 isolated runs, so a manifestation of the same environmental noise, not a logic bug, and not one of this phase's two named flakes to fix. T6 (two commits) is the `ui`/`ui-timing` Playwright split: the top-level `workers` (the actual worker-pool size — a project's own `workers` can only narrow it, discovered directly when the first post-split measurement silently ran at 2 workers despite each project's own `'100%'`) moved from `'50%'` to `'100%'`, `budgets.spec.ts`/`perf.spec.ts` moved into a new serial `ui-timing` project, and — extending D4 beyond the plan's own two files, since direct measurement showed it was not immune here the way the plan's own prototype found — `slick-grid.spec.ts`'s two "150 ms sandbox gate" tests (P22 Pass B C6/C12 T7) joined them, pulled out by title (`grep`/`grepInvert`) rather than a file move so the other 15 tests in that 110 s file stay parallel. T7 took `ipc-frontend` to `'100%'` too. T8 parallelized `typecheck`'s four independent projects via `sh -c '… & … & … & … & wait'` with each job's exit status collected explicitly. **`bun run test:ui` real numbers, clean confirming runs after every UI-side commit: 268.8 s and 275.0 s, 245/245 passed both times** — close to but ~4-7 % above the plan's own 258 s target, consistent with this box's own slower baseline; `budgets.spec.ts`'s own scroll-response p50 read 8-13 ms against its 12 ms bound across these runs (comfortably under it in most, at the edge in the noisier ones) — the same environmental variance the `grpcclient`/`shell` findings above describe, confirmed via `--no-deps` isolated runs of `ui-timing` alone (2 of 5 still exceeded 12 ms with absolutely nothing else running), not something this phase's own lever (removing *cross-file* contention) can fully close, and the bound was not loosened. **`bun run test:ipc:fe`: 10.5 s, 7/7 passed** (baseline 25.9 s, −59 %, matching the plan's ~10.1 s target). **`bun run typecheck`: sequential 15.2-15.8 s → concurrent 10.4-11.6 s in this worktree's own current background load** (both numbers shifted up from the plan's 11.0 s/9.7 s baseline, but the same ≈30 % relative saving holds). **`bun run test:unit`: 2.71 s, 682/682**, unchanged. **`bun run lint`: unchanged, sub-second.** T9 is this row. No test was deleted, skipped, or had an assertion loosened; the three environment-noise findings above are reported here rather than masked by a longer timeout or a looser bound. | The user asked for this directly, after being told this chapter's own test tiers currently take on the order of 10-20 minutes end to end (fast checks are already sub-second; `go test`'s real-container suites and `bun run test:ui`'s ~240-case suite are the two slow tiers). Sequenced after the whole-application review requested in the same conversation (and that review's own fix pass, if it finds anything), per this repo's one-phase-at-a-time discipline — a review that's about to touch code across the app should land first, so this phase's own timing measurements are taken against the tree's near-final state rather than one about to change under it |
